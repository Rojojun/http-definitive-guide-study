# 학습 원장: HTTP - The Definitive Guide

## 학습 계획

- 교재: `Http - The Definitive Guide.pdf`, 원서 초판(2002)
- 목표: 1장부터 순서대로 학습하고 현재 HTTP 및 Java/Spring/AWS 실무와 연결하기
- 학습자 배경: Java/Spring Boot 백엔드 4년 차, CloudFront·ALB·Nginx 사용 경험 일부
- 총 학습일: **42일 가안** - 사전 진단 Day 0 제외
- 하루 학습 시간: **최소 60분**
- 시작일: 2026-08-10
- 현재 진도: Day 10 완료, Part II 5장 웹 서버 완료
- 상태: Day 11 시작 대기, 6장 프록시 예정

### 42일 로드맵

| 기간 | 범위 | 중심 목표 |
|---|---|---|
| Day 1-8 | Part I, 1-4장 | 리소스·URL·메시지·연결 관리의 기초 모델 구축 |
| Day 9 | 누적 복습 1 | Part I 회상, 메시지 추적, 약점 보강 |
| Day 10-20 | Part II, 5-10장 | 서버·프록시·캐시·게이트웨이·로봇과 현대 HTTP/2·3 연결 |
| Day 21 | 누적 복습 2 | 중간자 아키텍처와 장애·성능 시나리오 통합 |
| Day 22-28 | Part III, 11-14장 | 식별·인증·보안과 현재 TLS·인증 관점 비교 |
| Day 29 | 누적 복습 3 | 보안 경계와 실무 설정 점검 |
| Day 30-34 | Part IV, 15-17장 | 엔터티·인코딩·국제화·콘텐츠 협상 |
| Day 35-39 | Part V, 18-21장 | 호스팅·배포·리다이렉션·로드밸런싱·로깅 |
| Day 40 | 현대화 종합 | 책 이후의 HTTP/2, HTTP/3, QUIC, TLS 1.3 정리 |
| Day 41 | 실무 전이 | Spring·CloudFront·ALB·Nginx 요청 경로 설계·장애 분석 |
| Day 42 | 최종 종합 | 전체 멘탈 모델, 남은 약점, 유지 복습 계획 |

이 일정은 고정 진도표가 아니라 가안이다. 핵심 개념이 부족하면 장을 나누고, 이미 아는 부분은 합쳐서 42일 안에서 재배분한다.

### 하루 60분 기본 리듬

- 10분: 이전 내용 노트 없이 회상
- 35분: 새 학습 단위와 최대 5턴의 추론
- 10분: Java/Spring/AWS 실무 적용 또는 장애 시나리오
- 5분: 단원·일일 요약과 학습 원장 갱신

## 진행 기록

| Day | 날짜 | 학습 단위 | 상태 | 확인된 근거 | 다음 복습 |
|---|---|---|---|---|---|
| 0 | 2026-08-10 | HTTP 요청 경로와 중간자 연결 경계 | 완료 | 분리된 연결, TCP 직전 상대, 전달 헤더를 단계적 교정 후 식별함 | 2026-08-11 |
| 1 | 2026-08-11 | 리소스, URI, 표현, 미디어 타입 | 완료 | 저장 구현과 리소스의 정체성을 분리하고 `Accept`가 같은 리소스의 표현 형식을 선택함을 새 사례에 적용함 | 2026-08-14 |
| 1 | 2026-08-12 | 확장자, `Content-Type`, 실제 표현 데이터 | 완료 | 파일명과 클라이언트 선언은 조작 가능하며 실제 바이트 파싱과 선언 일치 검증이 필요함을 업로드 사례에 적용함 | 2026-08-15 |
| 2 | 2026-08-12 | HTTP 트랜잭션과 연결 실패의 결과 불확실성 | 완료 | 하나의 TCP 연결과 세 HTTP 트랜잭션을 구분하고, 응답 유실 후 POST 재시도의 중복 위험을 도출함 | 2026-08-15 |
| 2 | 2026-08-12 | 메서드 안전성, 멱등성, 재시도 | 완료 | GET·PUT·DELETE·POST의 안전성과 멱등성을 구분하고 응답 유실 시 같은 멱등성 키를 재사용해야 함을 도출함 | 2026-08-15 |
| 3 | 2026-08-13 | HTTP/1.1 메시지 구조, 본문 경계, TCP 전달 관계 | 완료 | 요청줄·상태줄·헤더·빈 줄·본문을 구분하고 잘못된 `Content-Length`가 다음 메시지까지 오염시키는 과정과 TCP 유실·재전송의 차이를 설명함 | 2026-08-16 |
| 4 | 2026-08-14 | HTTP/2 멀티플렉싱과 HTTP/3 QUIC 스트림 | 완료 | HTTP/2 스트림 분리와 TCP HOL 블로킹을 구분하고 HTTP/3에서 유실 스트림만 대기하는 이유를 설명함 | 2026-08-17 |
| 4 | 2026-08-15 | 프록시, 캐시, 게이트웨이, 터널, 사용자 에이전트 | 완료 | 연결별 서버·클라이언트 역할과 중간자 기능을 분리하고 CloudFront 캐시 히트 시 원본이 호출되지 않음을 설명함 | 2026-08-18 |
| 5 | 2026-08-18 | URL 구성요소와 프래그먼트의 전달 경계 | 완료 | URL을 스킴·호스트·포트·경로·질의·프래그먼트로 분해하고 프래그먼트만 다른 두 URL이 서버에 같은 요청을 보냄을 설명함 | 2026-08-19 |
| 5 | 2026-08-18 | URL 스킴, 명시적 포트와 기본 포트 | 완료 | HTTPS의 기본 포트와 명시 포트를 구분하고 443 포트라도 `http` 스킴이면 TLS가 자동 적용되지 않음을 도출함 | 2026-08-19 |
| 5 | 2026-08-18 | 절대 URL과 상대 URL 해석 | 완료 | 루트 상대·경로 상대·후행 슬래시·`..`의 기준 경로 해석을 새 URL에 적용해 최종 절대 URL을 도출함 | 2026-08-19 |
| 5 | 2026-08-18 | URL 예약문자와 퍼센트 인코딩 | 완료 | 공백이 HTTP 요청줄을 깨뜨리는 이유를 설명하고 검색어 `java spring&boot`를 `java%20spring%26boot`로 인코딩함 | 2026-08-19 |
| 6 | 2026-08-19 | 안전한 URL 생성과 이중 인코딩 | 완료 | `%25`가 한 번 디코딩된 뒤 `%20`으로 남는 과정을 설명하고 원본 `100% spring`을 한 번만 인코딩함 | 2026-08-20 |
| 6 | 2026-08-19 | URL 정규화 | 완료 | 호스트 소문자화, HTTPS 기본 포트 제거, `.`·`..` 점 경로 제거를 새 URL에 적용해 정규 URL을 도출함 | 2026-08-20 |
| 6 | 2026-08-19 | URI·URL·URN과 주요 스킴 | 완료 | URL과 URN을 위치·접근 방식과 위치 독립적 이름으로 구분하고 `https`, `mailto`, `file`, `data`의 처리 방식을 예측함 | 2026-08-20 |
| 6 | 2026-08-20 | 2장 종합 전이 | 완료 | 정규화된 전체 URL, HTTP/1.1 요청 대상, Spring의 디코딩된 질의 값을 한 사례에서 모두 정확히 도출함 | 2026-08-23 |
| 7 | 2026-08-20 | 종단 간 헤더와 hop-by-hop 헤더 | 완료 | `Host`·`Accept`·`Authorization`의 최종 서버 역할과 `Connection`의 인접 연결 범위를 구분하고 Nginx 업스트림 연결 재사용을 예측함 | 2026-08-21 |
| 7 | 2026-08-20 | ETag와 조건부 GET 재검증 | 완료 | `If-None-Match` 일치 시 `304`와 전송 본문 없음, 불일치 시 `200`과 새 표현을 구분하고 클라이언트 캐시 재사용을 설명함 | 2026-08-21 |
| 7 | 2026-08-20 | `If-Match`와 수정 충돌 방지 | 완료 | 오래된 ETag의 수정 요청을 거부하고 최신 v8을 기준으로 재시도해 v9로 갱신하는 과정을 JPA 낙관적 락과 연결함 | 2026-08-21 |
| 7 | 2026-08-20 | 생성·삭제 응답의 상태 코드와 헤더 계약 | 완료 | `201 Location`을 자동 리다이렉션과 구분하고 `200` 본문 응답과 `204` 무본문 응답의 계약을 설명함 | 2026-08-21 |
| 7 | 2026-08-20 | 3장 종합 요청·응답 추적 | 완료 | 생성·재검증·조건부 수정·삭제 흐름에서 `201`, `304`, `412`, `204`와 헤더·본문·캐시 동작을 정확히 연결함 | 2026-08-23 |
| 8 | 2026-08-24 | TCP와 TLS의 역할, 새 연결 비용, 지속 연결 재사용 | 완료 | TCP의 순서 복구·재전송과 TLS의 암호화·서버 인증을 구분하고 3-way handshake 및 연결 재사용의 비용 절감을 설명함 | 2026-08-25 |
| 8 | 2026-08-24 | HTTP/1.1 순차·병렬·파이프라이닝과 HTTP/2 멀티플렉싱 비교 | 완료 | 제한된 병렬 지속 연결을 선택하고 HTTP/1.1 파이프라이닝의 응답 순서 제약과 HTTP/2 스트림 ID 기반 추월을 설명함 | 2026-08-25 |
| 8 | 2026-08-24 | 연결 종료, 본문 완전성, 안전한 재시도 | 완료 | TCP 종료와 HTTP 절단 판단을 분리하고 Content-Length·청크·close-delimited 경계를 비교한 뒤 GET과 멱등성 키 POST의 재시도 정책을 도출함 | 2026-08-25 |
| 8 | 2026-08-24 | 4장 핵심 범위 종합 전이 | 완료 | Nginx-Spring 재사용 연결에서 잘린 POST 응답을 HTTP가 판단하고 중복 방지 계약 없는 자동 재시도를 거부한 뒤 같은 멱등성 키를 제시함 | 2026-08-27 |
| 9 | 2026-08-25 | Part I 누적 복습 1: 연결 경계·URL·조건부 요청 | 완료 | 프록시 hop의 remoteAddr·XFF·Connection 범위를 정확히 도출하고, 명시적 8443·정규화 요청 대상을 교정 후 적용했으며 If-None-Match와 If-Match 결과를 구분함 | 2026-08-28 |
| 9 | 2026-08-25 | Part I 누적 복습 2: 완료·절단·재시도 | 완료 | 완전한 404와 절단 응답을 구분하고 서버 처리 성공과 응답 전달 실패를 분리했으며, 연결 유지 시 다음 응답 오염과 GET·POST 재시도 정책을 설명함 | 2026-08-28 |
| 9 | 2026-08-25 | 1장 누락 보충: 복합 웹페이지·HTTP 버전·Telnet | 완료 | 페이지 1개와 리소스 5개·트랜잭션 5개를 구분하고 HTTP/1.0 기본 종료와 HTTP/1.1 기본 지속 연결을 적용해 연결 수를 도출함 | 2026-08-28 |
| 9 | 2026-08-26 | 2장 누락 보충: URL 사용자 정보·경로 파라미터·자동 확장·스킴 | 완료 | userinfo와 노출 위험을 식별하고 세미콜론 경로 파라미터·질의·프래그먼트를 분리했으며 브라우저 URL 확장 후 요청줄·Host를 도출함 | 2026-08-29 |
| 9 | 2026-08-27 | 3장 누락 보충: 메서드·1xx·상태 코드·헤더 분류 | 완료 | HEAD·OPTIONS·TRACE의 역할을 구분하고 100 Continue와 최종 상태를 분리했으며 202·206·307·4xx·5xx 및 엔터티 헤더의 end-to-end 범위를 새 사례에 적용함 | 2026-08-30 |
| 9 | 2026-08-27 | 3장 누락 마무리: 헤더 연속 줄·HTTP/0.9 | 완료 | 과거 연속 줄을 이전 헤더 값에 결합해 `user admin`으로 해석하고 상태줄·헤더 존재 여부로 HTTP/0.9와 HTTP/1.x 응답을 구분함 | 2026-08-30 |
| 9 | 2026-08-27 | 4장 누락 보충: TCP 4-tuple과 소켓 | 완료 | HTTP 헤더 이전에 출발지·목적지 IP와 포트 조합으로 연결을 식별함을 설명하고 NAT 뒤 동일 공인 IP 연결도 변환된 출발지 포트로 구분함 | 2026-08-30 |
| 9 | 2026-08-28 | 4장 누락 보충: 트랜잭션 지연·Delayed ACK | 완료 | TCP ACK를 애플리케이션 완료와 분리해 특정 바이트까지 수신했다는 누적 확인으로 설명하고 Spring의 10초 처리 중에도 커널 ACK가 먼저 완료될 수 있음을 도출함 | 2026-08-31 |
| 9 | 2026-08-28 | 4장 누락 보충: Slow Start·Nagle/TCP_NODELAY | 완료 | 새 연결과 워밍된 지속 연결의 초기 전송량 차이를 혼잡 윈도우로 설명하고, TCP_NODELAY가 작은 메시지 지연을 줄이는 대신 세그먼트 수와 프로토콜 오버헤드를 늘릴 수 있음을 게임 사례에 적용함 | 2026-08-31 |
| 9 | 2026-08-28 | 4장 누락 보충: TIME_WAIT와 출발지 포트 고갈 | 완료 | 각 TCP hop에서 연결을 여는 측의 임시 출발지 포트가 4-tuple을 구성하며, Nginx에 TIME_WAIT가 쌓이면 Spring의 고정 목적지 포트가 아니라 Nginx의 출발지 포트 재사용이 제한됨을 도출함 | 2026-08-31 |
| 9 | 2026-08-28 | 4장 누락 보충: HTTP/1.0 Keep-Alive·dumb proxy·Proxy-Connection | 완료 | Connection을 end-to-end처럼 전달한 dumb proxy의 상호 대기와 timeout을 설명하고, Proxy-Connection의 우회 원리·다중 프록시 한계 및 hop별 독립 연결 정책을 도출함 | 2026-08-31 |
| 9 | 2026-08-28 | 4장 누락 보충: full·half close, FIN·RST, graceful close | 완료 | TCP를 독립된 양방향 바이트 스트림으로 설명하고 출력 FIN 이후에도 반대 방향 응답 수신이 가능함을 도출했으며, 미수신 데이터가 남은 강제 종료의 RST 위험과 graceful close 순서를 적용함 | 2026-08-31 |
| 9 | 2026-08-28 | Part I 1-4장 최종 통합 전이 | 완료 | 비기본 HTTPS URL의 요청줄·Host·프래그먼트, 프록시 remoteAddr·XFF, ETag 불일치의 200 응답, Content-Length 절단 및 Nginx TIME_WAIT·출발지 포트 압력을 하나의 경로에서 통합해 설명함 | 2026-08-31 |
| 10 | 2026-08-29 | 5장: 웹 서버 요청 처리 경계 | 완료 | 커널·Tomcat·Spring MVC·비즈니스 코드의 책임을 분리하고, Tomcat의 HTTP 파싱·Servlet 호출과 Spring의 라우팅·JSON 변환, Tomcat의 응답 바이트 전송을 순서대로 설명함 | 2026-09-01 |
| 10 | 2026-08-30 | 5장: 동시 연결 처리와 I/O 모델 | 완료 | 단일 스레드·워커 풀·다중화 I/O의 병목을 구분하고 Tomcat NIO가 JDBC 블로킹을 제거하지 않음을 설명했으며, WebFlux+JDBC 격리와 WebFlux+R2DBC의 대기 중 스레드 점유 차이를 적용함 | 2026-09-02 |
| 10 | 2026-08-31 | 5장: 리소스 매핑·document root·가상 호스팅 | 완료 | Host로 가상 호스트·애플리케이션을 선택한 뒤 요청 경로를 정적 파일 또는 Spring HandlerMapping에 연결함을 설명하고, 디코딩·정규화 후 document root 내부 여부를 검증해 경로 순회를 차단함 | 2026-09-03 |
| 10 | 2026-09-02 | 5장: MIME 타입·리다이렉션·접근 로그 | 완료 | Content-Type을 표현 해석 계약으로 설명하고 Feign 디코딩 불일치를 예측했으며, Location 기반 두 번째 요청과 trusted proxy가 정리한 XFF·Forwarded만 신뢰하는 로그 경계를 도출함 | 2026-09-05 |

## 지식 상태

### 확립된 내용

- DNS는 일반적으로 호스트 이름을 주소로 변환하며, URL 또는 스킴이 보통의 HTTP 포트를 결정한다.
- TCP는 full-duplex 통신이다.
- CloudFront, ALB, Nginx 같은 HTTP 중간자는 한 연결을 종료하고 다른 연결을 만들거나 재사용할 수 있다.
- 중간자는 호출자 쪽에서는 서버이고 다음 홉 쪽에서는 클라이언트다.
- Spring의 TCP 상대는 원래 사용자가 아니라 바로 앞의 연결 상대다.
- 전달 헤더는 독립된 연결 사이에서 원래 요청의 메타데이터를 운반한다.
- 리소스는 URI가 식별하는 개념적 대상이며 DB 행이나 응답 데이터 자체와 구분된다.
- 표현은 특정 시점의 리소스 상태를 전송 가능한 데이터로 만든 것이고, 미디어 타입은 그 데이터를 해석할 형식을 나타낸다.
- `Accept`는 리소스를 바꾸는 것이 아니라 서버가 제공할 표현 형식을 선택한다.
- URI 확장자는 리소스 식별자의 일부일 뿐 표현 형식을 보장하지 않으며, `Content-Type`은 표현의 형식과 처리 방법을 선언한다.
- 업로드 파일의 확장자와 요청 `Content-Type`은 클라이언트가 조작할 수 있으므로 실제 바이트 파싱 및 선언과의 일치 검증이 필요하다.
- 하나의 TCP 연결에서 여러 HTTP 요청·응답 트랜잭션을 순차적으로 처리할 수 있다.
- `4xx`나 `5xx` 응답도 유효한 HTTP 응답을 받았다면 프로토콜 트랜잭션은 완료된 것이며 애플리케이션 결과와 구분해야 한다.
- 응답 전에 연결이 끊기면 클라이언트는 서버 처리 여부를 알 수 없고, 비멱등 요청의 무조건 재시도는 중복 처리를 만들 수 있다.
- 안전성은 클라이언트가 상태 변경을 요청하는지, 멱등성은 반복 실행의 의도된 최종 효과가 한 번 실행과 같은지를 뜻한다.
- `GET`은 안전·멱등, `PUT`과 `DELETE`는 비안전·멱등, 일반적인 `POST`는 비안전·비멱등이다.
- 비멱등 요청의 재시도는 첫 요청과 같은 멱등성 키로 식별하고 서버가 원자적으로 중복 처리를 차단해야 한다.
- HTTP/1.1 메시지는 시작줄, 0개 이상의 헤더, 빈 줄, 선택적 본문으로 구성된다.
- 요청줄은 메서드·요청 대상·HTTP 버전이고, 상태줄은 HTTP 버전·상태 코드·사유 구문이다.
- 본문 경계는 JSON 문법이 아니라 HTTP 프레이밍 정보로 결정되며, 잘못된 `Content-Length`는 대기·절단·다음 메시지 파싱 오염을 일으킨다.
- HTTP/2는 하나의 TCP 연결에서 프레임의 스트림 ID로 여러 요청·응답을 멀티플렉싱하지만 TCP 계층의 HOL 블로킹은 남는다.
- HTTP/3는 QUIC의 독립 스트림을 사용하므로 한 스트림의 패킷 유실이 다른 스트림의 진행을 막지 않는다.
- 프록시는 같은 HTTP 생태계에서 중계와 정책을 수행하고, 캐시는 저장된 응답을 재사용하며, 전통적 게이트웨이는 서로 다른 프로토콜·인터페이스를 연결한다.
- CONNECT 터널은 내부 TLS 메시지를 해석하지 않고 양방향 바이트 통로를 제공한다.
- 서버와 클라이언트 역할은 컴포넌트에 고정되지 않고 각 인접 HTTP 연결을 기준으로 결정된다.
- URL은 스킴·호스트·포트·경로·질의·프래그먼트로 분해할 수 있으며, 프래그먼트는 클라이언트에서 처리되어 HTTP 요청으로 서버에 전달되지 않는다.
- URL 스킴은 접근 방식과 생략 시 기본 포트를 결정하며, `http`는 기본 80, `https`는 기본 443이고 명시적 포트는 기본값을 덮어쓴다.
- 포트 번호만으로 TLS가 활성화되지 않으며 `http://host:443`은 평문 HTTP를 시도하므로 TLS를 기대하는 서버와 통신에 실패할 수 있다.
- 상대 URL은 기준 URL을 바탕으로 절대 URL로 해석되며, `/path`는 오리진 루트부터, `path`는 기준 경로의 마지막 조각을 기준으로, `..`는 상위 경로로 해석된다.
- 기준 URL의 후행 슬래시는 마지막 경로 조각을 디렉터리처럼 유지할지 교체할지를 바꾼다.
- 퍼센트 인코딩은 URL 구성요소의 데이터에 들어간 공백·예약문자를 `%HH` 형태의 바이트 값으로 표현해 문법 구분자와 충돌하지 않게 한다.
- 질의 값의 `&`는 `%26`으로 인코딩해야 하나 파라미터 사이의 구분자 `&`는 그대로 유지하므로 URL 전체가 아니라 구성요소의 값을 인코딩해야 한다.
- URL 생성에서는 인코딩 책임자를 하나로 정해야 하며, 원본 값을 받는 URI 빌더에 미리 인코딩한 값을 넘기면 `%`가 `%25`로 바뀌어 이중 인코딩될 수 있다.
- URL 정규화는 호스트 대소문자, 스킴의 기본 포트, 경로의 `.`·`..` 같은 의미상 중복을 규칙에 따라 제거해 동등한 URL을 일관된 형태로 만든다.
- 책의 구분에서 URI는 상위 개념이고 URL은 위치와 접근 방법으로, URN은 위치와 무관한 이름으로 리소스를 식별한다.
- 스킴은 식별자를 처리할 방식과 핸들러를 결정하며 모든 URI가 HTTP 요청을 만드는 것은 아니다.
- 정규화된 전체 URL에는 프래그먼트가 유지될 수 있지만 HTTP 요청 대상에서는 제거되며, 서버 프레임워크는 퍼센트 인코딩된 질의 값을 일반적으로 한 번 디코딩한다.
- `Host`, `Accept`, `Authorization`은 일반적으로 최종 서버의 처리에 필요한 종단 간 정보이고, `Connection`은 현재 인접 연결에만 적용되는 hop-by-hop 헤더다.
- 프록시 양쪽의 전송 연결은 각각 양방향이지만 서로 독립된 별도 연결이며, 한쪽의 `Connection: close`가 다른 쪽 연결 종료를 강제하지 않는다.
- 조건부 GET에서 `If-None-Match`가 현재 ETag와 같으면 서버는 `304 Not Modified`와 전송 본문 없음으로 응답하고 클라이언트는 기존 캐시 본문을 재사용한다.
- ETag가 다르면 표현이 변경된 것이므로 서버는 `200 OK`, 새 ETag, 새 본문을 반환한다.
- `If-Match`는 서버의 현재 ETag와 요청 값이 정확히 일치할 때만 상태 변경을 허용하며, 불일치하면 일반적으로 `412 Precondition Failed`로 오래된 쓰기를 거부한다.
- ETag 기반 조건부 수정은 JPA `@Version` 낙관적 락처럼 lost update를 방지하며, 충돌 후 최신 표현을 조회·병합한 뒤 최신 ETag로 재시도한다.
- `201 Created`의 `Location`은 새로 생성된 리소스의 URI를 식별하며 3xx의 `Location`처럼 자동 리다이렉션을 명령하지 않는다.
- 성공한 삭제는 결과 표현을 반환하면 `200`, 반환할 콘텐츠가 없으면 `204`를 사용할 수 있으며 `204` 응답에는 콘텐츠를 포함할 수 없다.
- TCP는 신뢰성 있는 순서화된 바이트 스트림과 유실 재전송을 담당하고, TLS는 TCP 위에서 암호화·무결성·서버 인증을 제공한다.
- `http`는 HTTP를 TCP 위에서 직접 전송하고 `https`는 HTTP와 TCP 사이에 TLS를 추가하며, 지속 연결 재사용 시 새 TCP·TLS 핸드셰이크를 반복하지 않는다.
- 하나의 지속 TCP 연결에서 여러 HTTP 트랜잭션을 처리할 수 있고, 프록시 양쪽 연결의 생성·재사용·종료는 hop마다 독립적이다.
- HTTP/1.1 비파이프라인 순차 방식은 이전 응답 후 다음 요청을 보내므로 지연이 누적되고, 파이프라이닝은 요청을 연속 전송해도 응답 순서를 유지해야 한다.
- 제한된 수의 병렬 지속 연결은 순차 대기를 줄이면서 연결 수립·소켓 상태 비용을 통제한다.
- HTTP/2는 프레임의 스트림 ID로 응답을 구분해 스트림 간 완료 순서를 독립적으로 허용하지만 각 스트림 내부 순서는 유지한다.
- TCP는 연결 종료 사실만 알리고, HTTP는 `Content-Length` 또는 청크 종료 경계와 실제 수신 바이트를 비교해 메시지 절단 여부를 판단한다.
- 길이 정보 없이 연결 종료로 본문 끝을 표시하면 정상 종료와 조기 단절을 길이만으로 구분하기 어렵고 연결을 재사용할 수 없다.
- 응답을 받지 못한 요청은 서버 처리 여부가 불확실하므로 멱등 GET은 일반적으로 재시도할 수 있지만 주문 생성 POST는 같은 멱등성 키 같은 중복 방지 장치가 필요하다.
- 웹페이지 하나는 HTML과 CSS·JavaScript·이미지 등 여러 리소스와 HTTP 트랜잭션으로 구성될 수 있으며 TCP 연결 수와 트랜잭션 수는 1:1이 아니다.
- HTTP/1.0은 기본적으로 트랜잭션 뒤 연결을 닫고 Keep-Alive는 비표준 확장이지만, HTTP/1.1은 지속 연결이 기본이고 `Connection: close`로 종료를 명시한다.
- Telnet 예시는 raw TCP 연결 위에 HTTP 텍스트 메시지를 직접 보내는 관찰 실습이며 Telnet이 HTTP 프로토콜인 것은 아니다.
- URL의 `username:password@` userinfo는 권한부에 속하지만 로그·기록 등에 자격 증명을 노출할 수 있어 현대 HTTP 서비스에서 사용하지 않는다.
- 세미콜론 경로 파라미터는 바로 앞 경로 조각에 붙고, 질의는 `?`, 프래그먼트는 `#`부터 시작하며 Spring의 경로 변수와도 구분된다.
- URL 자동 확장은 클라이언트가 축약 입력을 완전한 URL로 만든 뒤 DNS·HTTP를 시작하는 편의 기능이므로 원래 축약 문자열은 서버에 전달되지 않는다.
- URL 스킴은 식별자를 처리할 프로토콜·핸들러를 결정하며 `ftp`, `mailto`, `file`, `data`, `news`, `telnet`은 HTTP 요청을 만들지 않는다.

### 교정한 오해

- DNS가 HTTP 호스트와 포트를 모두 매핑한다고 생각했다.
- 클라이언트 연결 하나가 CloudFront와 ALB를 지나 Spring까지 그대로 이어진다고 생각했다.
- 종료된 클라이언트 연결이 원본 서버 연결로 재사용된다고 생각했다.
- 프록시가 요청을 전달할 때 자기 IP를 `X-Forwarded-For`에 추가한다고 생각했다.
- 리소스를 반환 데이터로, 미디어 타입을 리소스 표현 메서드로 생각했다.
- 업로드 검증에서 요청 `Content-Type`의 불신만 먼저 식별했으나 파일 확장자도 동일하게 신뢰할 수 없음을 교정했다.
- `404 Not Found`를 HTTP 트랜잭션 실패로 보았으나, 프로토콜 교환의 완료와 애플리케이션 결과 실패를 분리했다.
- 상태를 변경하는 `GET`을 안전하다고 보았으나, 메서드의 선언된 의미와 실제 구현 계약을 분리하고 해당 구현은 안전성과 멱등성을 모두 위반함을 교정했다.
- 응답 유실 재시도 전에 첫 처리를 롤백한다고 생각했으나, 커밋 여부를 클라이언트가 알 수 없으므로 같은 멱등성 키로 기존 처리 결과를 식별해야 함을 보강했다.
- 응답 상태줄의 `404`를 요청 메서드로 보았으나 상태 코드와 사유 구문으로 교정했다.
- JSON의 닫는 문자나 실제 도착 바이트로 본문 끝을 판단한다고 생각했으나 HTTP 파서는 선언된 메시지 경계를 우선 사용함을 교정했다.
- HTTP/2에서 유실된 B 뒤에 도착한 A 스트림이 즉시 전달된다고 생각했으나, HTTP/2 파서 전에 TCP가 연결 전체의 바이트 순서를 복구하므로 A도 대기함을 교정했다.
- 프록시는 변환하지 않고 게이트웨이만 라우팅한다고 생각했으나, 프록시도 헤더·경로·정책·라우팅을 수행하며 전통적 게이트웨이의 핵심은 시스템 경계의 번역임을 교정했다.
- CONNECT 중간자를 게이트웨이로 추측했으나 연결 이후 내부 바이트를 해석하지 않는 양방향 터널임을 교정했다.
- Spring을 CloudFront에도 직접 서버라고 보았으나 각 중간자가 연결을 분리하므로 Spring의 직접 상류 상대는 API Gateway임을 교정했다.
- URI가 식별하는 리소스와 JSON 표현 및 `Content-Type` 선언을 다시 혼동했으나, 개념적 대상·표현 데이터·형식 메타데이터로 구분했다.
- 프래그먼트가 다른 URL의 서버 요청이 같다는 점은 이해했으나 이를 URN이 같다고 표현한 용어 혼동을 교정했다.
- 경로 상대 참조 `cart`가 호스트를 바꾼다고 추측했으나 현재 오리진을 유지한 채 기준 경로와 병합됨을 교정했다.
- `..` 하나가 루트까지 이동한다고 추측했으나 경로 계층을 한 단계만 제거함을 교정했다.
- 공백이 든 요청줄의 오류를 TCP 문제로 표현했으나 TCP는 바이트를 전달하고 HTTP 파서가 요청줄 토큰 수의 오류를 검출함을 교정했다.
- URI 빌더가 인코딩을 담당하는 상황에서도 미리 인코딩한 값을 넘기려 했으나, 빌더 계약에 맞춰 원본 값을 전달하고 한 번만 인코딩해야 함을 교정했다.
- 점 경로에서 `b/..` 뒤의 `c`까지 제거해 `/a/b`로 계산했으나, `b/..`만 상쇄되고 뒤의 `c`는 유지되어 `/a/c`가 됨을 교정했다.
- `keep-alive`를 한쪽 방향을 열어두는 의미로 보았으나, 양방향 연결을 요청·응답 후에도 유지해 다음 HTTP 트랜잭션에 재사용하는 의미로 교정했다.
- 서로 다른 프록시 구간을 `양방향 연결` 관계로 표현했으나, 각 연결은 모두 양방향이면서 서로 독립적으로 관리되는 별도 연결임을 교정했다.
- 캐시가 있으므로 서버가 `200`과 같은 본문을 다시 보낸다고 생각했으나, ETag 일치 시 `304`로 본문 전송을 생략하고 클라이언트가 캐시 본문을 사용함을 교정했다.
- `http`에서 HTTP 자체가 생략될 수 있다고 혼동했으나 `http`는 TLS만 생략되고 HTTP와 TCP는 유지됨을 교정했다.
- 대량 연결의 연결 수립 비용과 유지 비용을 섞었으나, 3-way handshake는 수립 비용이고 소켓·버퍼·파일 디스크립터·TCP 상태는 유지 비용으로 구분했다.
- HTTP/1.1 파이프라이닝을 병렬 응답과 혼동할 수 있었으나 요청은 연속 전송해도 응답은 요청 순서를 지켜야 함을 교정했다.
- `Content-Length`보다 짧게 받은 뒤 연결이 종료된 경우 다음 응답에서 부족한 바이트를 가져온다고 생각했으나, 이미 연결이 끝났으므로 HTTP가 절단 응답으로 판단함을 교정했다.
- 연결 종료 후 GET과 POST 모두 재시도할 수 없다고 보았으나, 계약대로 구현된 멱등 GET과 중복 방지 없는 비멱등 POST의 재시도 위험을 구분했다.
- `https://host:8443` 누적 복습에서 명시적 포트를 443으로 바꾸고 Host에서 포트를 생략했으나, 비기본 명시 포트 8443을 URL·접속 대상·Host에 유지하도록 교정했다.
- `If-None-Match` 불일치를 `412`로 답했으나 조건부 GET 불일치는 `200`과 새 표현이고, `412`는 `If-Match` 수정 전제 불일치임을 다시 구분했다.
- 절단된 GET 응답을 서버 처리 실패로 단정했으나 서버 로직은 성공하고 응답 전달만 실패할 수 있으며, 클라이언트가 확정하는 것은 완전한 표현을 받지 못했다는 사실임을 교정했다.
- GET 구현에 숨은 부작용이 있을 가능성을 이유로 자동 재시도를 거부하려 했으나, 재시도 정책은 표준 메서드 계약을 기준으로 하고 부작용 있는 GET은 서버 계약 위반임을 교정했다.

### 보강할 내용

- 임의의 프록시 경로에서 `remoteAddr`와 `X-Forwarded-For`를 힌트 없이 도출하기
- 전송 계층의 직전 상대 정보와 HTTP 계층의 전달 메타데이터 구분하기
- 클라이언트가 조작할 수 있는 전달 헤더와 신뢰 프록시 경계 이해하기

## 복습 큐

| 예정일 | 회상 질문 | 최근 결과 |
|---|---|---|
| 2026-08-11 | `Client -> ALB -> Spring`에서 `remoteAddr`와 `X-Forwarded-For`를 도출하고 누가 어떤 값을 추가하는지 설명하라. | 교정 후 `remoteAddr`는 TCP 직전 상대, `X-Forwarded-For`는 프록시가 관찰한 상류 상대를 담는 HTTP 헤더라고 설명함 |
| 2026-08-13 | `Client -> CloudFront -> ALB -> Nginx -> Spring`에서 각 연결의 소유자와 전달 헤더 체인을 도출하라. | 힌트 후 성공 |
| 2026-08-17 | CloudFront-ALB 연결 재사용이 사용자 연결의 생명주기와 독립적인 이유를 설명하라. | 힌트 후 성공 |
| 2026-08-15 | `404` 응답과 응답 전 TCP 연결 종료 중 어느 경우에 HTTP 트랜잭션이 완료되는지 설명하라. | 새 학습 후 전이 성공 |
| 2026-08-15 | POST 응답이 유실된 뒤 같은 멱등성 키로 재시도할 때 서버가 중복 처리를 막는 과정을 설명하라. | 서버 내부 조회·선점 로직까지 이해함 |
| 2026-08-19 | 질의와 프래그먼트가 각각 서버 요청과 클라이언트 처리에 어떻게 관여하는지 설명하라. | 새 URL에서 프래그먼트만 다른 두 주소가 같은 서버 요청을 만든다고 설명함 |
| 2026-08-19 | `https://host:8443`과 `http://host:443`에서 스킴과 포트가 각각 무엇을 결정하는지 설명하라. | 8443에서도 HTTPS가 유지되고 443이라도 http 스킴이면 HTTPS가 아님을 설명함 |
| 2026-08-19 | 기준 URL의 후행 슬래시와 `/`, `..`가 상대 URL 해석 결과를 어떻게 바꾸는지 설명하라. | `../../cart`를 새 계층 경로에 적용해 정확한 절대 URL을 도출함 |
| 2026-08-19 | 검색어에 공백과 `&`가 함께 있을 때 URL 전체가 아니라 질의 값만 퍼센트 인코딩해야 하는 이유를 설명하라. | `java spring&boot`를 `java%20spring%26boot`로 정확히 변환함 |
| 2026-08-20 | URI 빌더가 원본 값을 인코딩하는 계약일 때 미리 인코딩한 값을 넘기면 왜 `%2520`이 되는지 설명하라. | `100% spring`을 원본으로 넘기고 `100%25%20spring`으로 한 번만 인코딩함 |
| 2026-08-20 | 호스트 대소문자와 기본 포트 및 `.`·`..`가 섞인 URL을 정규화하라. | `https://API.EXAMPLE.com:443/v1/users/../orders/./42`를 `https://api.example.com/v1/orders/42`로 정확히 정규화함 |
| 2026-08-20 | URL과 URN을 위치·접근 방식과 위치 독립적 이름으로 구분하고 `mailto`, `file`, `data`가 HTTP 요청을 만드는지 설명하라. | HTTPS만 웹 서버에 HTTP 요청을 보내며 나머지 스킴의 처리 방향을 정확히 예측함 |
| 2026-08-23 | 하나의 복합 URL을 정규화하고 전체 URL·HTTP 요청 대상·Spring 디코딩 값을 구분하라. | 대문자 호스트, 기본 포트, 점 경로, 이중 인코딩, 프래그먼트가 섞인 사례를 정확히 해결함 |
| 2026-08-21 | `Client → Nginx → Spring`에서 `Connection: close`와 `Accept`의 전달 범위 및 두 TCP 연결의 관계를 설명하라. | 동작은 정확히 예측했으나 hop-by-hop과 독립된 별도 연결이라는 용어·원인 표현에 반복 교정이 필요했음 |
| 2026-08-21 | `If-None-Match`와 현재 ETag의 일치·불일치에 따른 상태 코드, 전송 본문, 캐시 사용을 설명하라. | 단계적 교정 후 일치 시 `304`와 캐시 v7 본문 재사용을 정확히 설명함 |
| 2026-08-21 | 두 사용자가 같은 ETag를 조회한 뒤 순차 수정할 때 `If-Match`, `412`, 최신 버전 재시도를 설명하라. | v7 충돌 후 최신 v8을 보내야 함을 도출하고 `If-Match`의 정확한 일치 조건을 설명함 |
| 2026-08-21 | 생성 성공과 삭제 성공에서 `201 Location`, `200`, `204`의 상태 코드·헤더·본문 계약을 비교하라. | `201`의 위치 식별과 `204`에 본문을 포함할 수 없는 모순을 정확히 설명함 |
| 2026-08-23 | 생성·재검증·수정 충돌·삭제의 연속 흐름에서 상태 코드와 헤더·본문·캐시 동작을 추적하라. | 선택지를 축소한 뒤 `304` 무본문·캐시 사용과 `412` 수정 거부를 정확히 구분함 |

## Day 0 단원 요약

- 결과: 리버스 프록시를 통과하는 HTTP 요청의 hop-by-hop 연결 모델을 처음 구축했다.
- 멘탈 모델: 인접한 두 컴포넌트가 별도의 전송 연결을 소유하고, HTTP 중간자가 연결 사이에서 메시지를 전달하거나 변환한다.
- 실무 연결: CloudFront-ALB-Spring 구조에서 Spring은 ALB를 직접 보며, 신뢰할 수 있는 HTTP 헤더가 선택된 사용자 정보를 보존한다.
- 핵심 교정: `X-Forwarded-For`에는 각 신뢰 프록시가 자신이 관찰한 상류 상대를 추가하며, 최종 직전 상대는 TCP 원격 주소로 확인한다.
- 회상 질문: 임의의 4단계 프록시 경로를 그리고 연결 소유권과 전달 헤더 체인을 함께 표시하라.

## Day 1 단원 1 요약

- 결과: 리소스의 정체성과 HTTP로 전달되는 표현을 구분했다.
- 멘탈 모델: URI는 개념적 대상을 식별하고, 서버는 그 시점의 상태를 JSON, HTML, CSV 같은 표현으로 만들어 전송한다.
- 실무 연결: 동일한 Spring 엔드포인트가 `Accept`에 따라 여러 미디어 타입의 표현을 제공할 수 있다.
- 핵심 교정: 리소스는 반환 데이터나 DB 행 자체가 아니며, 미디어 타입은 표현 메서드가 아니라 데이터 해석 형식이다.
- 회상 질문: DB를 교체하거나 JSON을 HTML로 바꿔도 같은 리소스일 수 있는 이유를 설명하라.

## Day 1 단원 2 요약

- 결과: URI 확장자, `Content-Type`, 실제 표현 데이터의 역할과 신뢰 경계를 구분했다.
- 멘탈 모델: 확장자는 식별자의 일부이고, `Content-Type`은 표현 형식 선언이며, 본문 바이트가 실제 표현이다.
- 실무 연결: Spring 파일 업로드에서는 확장자와 클라이언트 `Content-Type`을 허용 목록에만 사용하고 실제 바이트를 파싱한 뒤 선언과의 일치를 검증한다.
- 핵심 교정: 확장자와 요청 헤더는 모두 클라이언트가 조작할 수 있으며 허용 형식이라도 선언과 실제 형식이 다르면 기본적으로 계약 위반으로 거부한다.
- 회상 질문: 파일 확장자와 `Content-Type`이 모두 실제 바이트와 다를 때 무엇을 신뢰하고 어떻게 처리해야 하는가?

## Day 2 단원 1 요약

- 결과: TCP 연결, HTTP 트랜잭션, 애플리케이션 처리 결과를 서로 구분했다.
- 멘탈 모델: 한 TCP 연결은 여러 요청·응답 교환을 운반할 수 있고, 유효한 상태 코드 응답을 받으면 HTTP 트랜잭션은 완료된다.
- 실무 연결: 서버가 POST 처리를 끝낸 뒤 응답이 유실되면 클라이언트에게 결과는 불확실하며, 단순 재시도는 중복 주문을 만들 수 있다.
- 핵심 교정: `404`는 HTTP 트랜잭션 자체의 실패가 아니라 완료된 응답이 전달한 애플리케이션 결과다.
- 회상 질문: POST 응답이 유실됐을 때 클라이언트가 실패와 성공 어느 쪽도 확정할 수 없는 이유는 무엇인가?

## Day 2 단원 2 요약

- 결과: HTTP 메서드의 안전성과 멱등성을 구분하고 재시도 정책에 적용했다.
- 멘탈 모델: 안전성은 상태 변경 의도의 부재이고, 멱등성은 반복 실행의 의도된 최종 효과가 동일한 성질이다.
- 실무 연결: 응답 유실 뒤 비멱등 POST를 재시도할 때는 최초 요청과 같은 멱등성 키를 보내고 서버가 처리 상태를 원자적으로 관리한다.
- 핵심 교정: 상태를 변경하는 GET 구현은 GET의 안전·멱등 계약을 위반하며, 응답 유실은 첫 처리를 자동으로 롤백할 근거가 아니다.
- 회상 질문: 서버가 주문을 커밋한 뒤 응답이 유실됐을 때 같은 멱등성 키가 중복 주문을 막는 과정을 설명하라.

## Day 2 일일 요약

- 상태: Day 2/42 완료
- 완료 단원:
  - HTTP 트랜잭션과 연결 실패의 결과 불확실성
  - HTTP 메서드의 안전성, 멱등성, 재시도
- 확립한 내용:
  - 하나의 TCP 연결은 여러 HTTP 트랜잭션을 운반할 수 있다.
  - `404`나 `500`도 유효한 응답을 받으면 HTTP 트랜잭션은 완료된다.
  - 프로토콜 교환 완료, 애플리케이션 결과, 서버 내부 커밋 여부는 서로 다른 판단 축이다.
  - `GET`은 안전·멱등, `PUT`과 `DELETE`는 비안전·멱등, 일반적인 `POST`는 비안전·비멱등이다.
  - 응답 유실 뒤 재시도는 최초 요청과 같은 멱등성 키를 사용하고, 서버가 키를 조회·선점하여 중복 실행을 막는다.
- 확인된 근거:
  - 단일 연결의 세 요청·응답을 세 HTTP 트랜잭션으로 정확히 구분했다.
  - `DELETE`의 응답이 달라도 최종 상태가 같아 멱등함을 설명했다.
  - POST 응답 유실이 중복 주문을 만들 수 있음을 예측하고 같은 멱등성 키 재사용을 도출했다.
- 보강할 내용:
  - 멱등성 키 기록과 비즈니스 데이터 변경을 원자적으로 묶는 구현 방식
  - 메서드의 표준 의미와 애플리케이션 구현이 어긋날 때의 캐시·재시도 영향

## Day 3 단원 1 요약

- 결과: HTTP/1.1 요청과 응답을 시작줄, 헤더, 빈 줄, 본문으로 분해하고 메시지 경계를 설명했다.
- 멘탈 모델: HTTP 파서는 본문 형식의 문법이 아니라 프로토콜 프레이밍 정보로 현재 메시지의 끝을 판단한다.
- 실무 연결: `Content-Length`가 실제보다 크면 대기하거나 다음 메시지를 흡수하고, 작으면 남은 바이트가 다음 메시지 파싱을 오염시킨다.
- 핵심 교정: 응답의 시작줄은 메서드가 아니라 상태 코드와 사유 구문을 포함하며, JSON의 `}`는 HTTP 본문 종료 표지가 아니다.
- 회상 질문: 지속 연결에서 실제보다 1바이트 큰 `Content-Length`가 두 응답을 모두 깨뜨리는 과정을 설명하라.

## Day 3 일일 요약

- 상태: Day 3/42 완료, 1장은 진행 중
- 완료 단원:
  - HTTP/1.1 요청줄·상태줄·헤더·빈 줄·본문
  - `Content-Length` 기반 본문 경계와 지속 연결 오염
  - TCP 세그먼트 분할, 순서 복구, 재전송과 HTTP 프레이밍의 역할 분담
- 확립한 내용:
  - TCP는 바이트 순서와 유실 복구를 담당하지만 HTTP 메시지 경계를 모른다.
  - HTTP 파서는 TCP 세그먼트 개수가 아니라 시작줄, 헤더 종료, 본문 길이로 메시지 완성을 판단한다.
  - TCP 유실은 시퀀스 번호의 빈 구간이라 재전송되지만 잘못된 `Content-Length`는 HTTP 계층의 경계 오류라 TCP가 복구하지 않는다.
- 1장 미완료 범위:
  - HTTP 버전 변화 개관
  - 프록시, 캐시, 게이트웨이, 터널, 에이전트 구분
  - 1장 종합 전이 확인

## Day 4 단원 1 요약

- 결과: HTTP/2의 멀티플렉싱과 HTTP/3의 QUIC 스트림이 해결하는 막힘의 경계를 구분했다.
- 멘탈 모델: HTTP/2는 TCP 위에서 스트림을 나누므로 HTTP 수준 동시성은 얻지만 TCP 순서 복구는 연결 전체를 막을 수 있다. HTTP/3는 전송 계층부터 스트림을 독립시킨다.
- 실무 연결: HTTP/2에서 한 TCP 세그먼트 유실이 여러 요청의 지연으로 관측될 수 있고, HTTP/3에서는 유실된 QUIC 스트림과 다른 요청이 계속 진행할 수 있다.
- 핵심 교정: HTTP/2의 스트림 ID는 TCP가 순서대로 전달한 뒤에야 파싱되므로 TCP HOL 블로킹을 우회할 수 없다.
- 회상 질문: HTTP/2의 B 프레임이 유실됐을 때 뒤에 도착한 A 프레임도 대기하는 계층 순서를 설명하라.

## Day 4 단원 2 요약

- 결과: 프록시, 캐시, 게이트웨이, 터널, 사용자 에이전트의 역할과 겹치는 경계를 구분했다.
- 멘탈 모델: 연결별로 서버·클라이언트 역할을 먼저 정하고, 그 위에서 중계·저장·번역·불투명 전달 같은 기능을 구분한다.
- 실무 연결: CloudFront는 캐시 히트 시 저장된 응답을 직접 제공하고, 미스 시 리버스 프록시로 원본에 전달한다. Spring은 API Gateway에는 서버이고 결제 API에는 클라이언트다.
- 핵심 교정: 프록시도 라우팅과 메시지 변환을 할 수 있으며, 캐시 히트에서는 프록시 중계 기능이 아니라 저장 응답 제공 기능이 실행된다.
- 회상 질문: `Chrome → CloudFront → API Gateway → Spring → 결제 API`에서 각 인접 연결의 서버·클라이언트와 구성요소 기능을 설명하라.

## 1장 요약

- 개념 사슬: 리소스 → URI로 식별 → 표현과 미디어 타입 → 요청·응답 트랜잭션 → TCP 연결 → HTTP 중간자.
- 핵심 불변식:
  - 리소스의 정체성과 표현 및 저장 구현은 서로 다르다.
  - HTTP 메시지 경계는 본문 형식이 아니라 프로토콜 프레이밍으로 결정된다.
  - TCP는 순서와 재전송을 담당하고 HTTP는 메시지 의미와 경계를 해석한다.
  - 메서드 안전성·멱등성은 재시도와 중복 처리 정책의 근거가 된다.
  - 서버·클라이언트 역할은 인접 연결마다 달라진다.
- 현대 확장: HTTP/2는 TCP 위에서 스트림을 멀티플렉싱하고, HTTP/3는 QUIC 스트림으로 유실 영향을 격리한다.
- 실무 적용: CloudFront·API Gateway·Spring·외부 API 경로에서 연결 경계, 캐시 히트, 중간자 기능을 분리해 분석한다.
- 누적 회상: 캐시 미스와 히트 각각에서 요청 경로, 연결 상대, 응답 출처를 도출하라.

## Day 4 일일 요약

- 상태: Day 4/42 완료, 1장 완료
- 완료 단원:
  - HTTP/2 멀티플렉싱과 TCP HOL 블로킹
  - HTTP/3 QUIC 스트림의 독립성
  - 프록시, 캐시, 게이트웨이, 터널, 사용자 에이전트
  - 1장 종합
- 확립한 내용:
  - HTTP/2는 하나의 TCP 연결에서 여러 스트림을 멀티플렉싱하지만 TCP 순서 복구의 영향은 연결 전체에 미칠 수 있다.
  - HTTP/3는 QUIC 스트림을 사용해 한 스트림의 유실이 다른 스트림의 진행을 막지 않게 한다.
  - CloudFront는 캐시 히트 시 저장된 응답을 제공하고, 캐시 미스 시 원본으로 요청을 중계한다.
  - 프록시는 HTTP 중계와 정책을, 전통적 게이트웨이는 시스템 경계 번역을, 터널은 불투명한 양방향 바이트 전달을 담당한다.
  - 서버·클라이언트 역할은 각 인접 연결마다 달라진다.
- 확인된 근거:
  - HTTP/2의 멀티플렉싱 위치와 TCP HOL 블로킹의 선후 관계를 교정 후 설명했다.
  - CloudFront의 프록시 기능과 캐시 히트 동작을 구분했다.
  - Spring이 API Gateway에는 서버이고 외부 결제 API에는 클라이언트임을 도출했다.
- 다음 복습:
  - HTTP/2와 HTTP/3에서 B 스트림 패킷 유실이 A 스트림에 미치는 영향을 비교한다.
  - CloudFront 캐시 히트와 미스의 요청 경로를 각각 그린다.

## Day 5 단원 1 요약

- 결과: URL을 스킴, 호스트, 포트, 경로, 질의, 프래그먼트로 분해하고 서버 전달 경계를 구분했다.
- 멘탈 모델: 스킴은 접근 방식, 호스트와 포트는 접속 대상, 경로와 질의는 서버의 요청 대상을 구성하며 프래그먼트는 클라이언트에서만 처리된다.
- 실무 연결: Spring은 질의를 `@RequestParam`으로 받을 수 있지만 `#fragment`는 HTTP 요청에 포함되지 않아 서버 로그나 컨트롤러에서 볼 수 없다.
- 핵심 교정: 리소스는 JSON이나 URI 문자열 자체가 아니며, 프래그먼트가 다른 두 URL은 전체 URI는 다르지만 서버에 전달되는 요청 대상은 같고 URN과는 관계없다.
- 회상 질문: 같은 경로에서 질의만 바꾼 경우와 프래그먼트만 바꾼 경우 서버가 관찰하는 요청을 비교하라.

## Day 5 단원 2 요약

- 결과: URL 스킴과 포트가 각각 통신 방식과 접속 지점을 결정하는 역할을 구분했다.
- 멘탈 모델: 스킴은 HTTP인지 HTTPS인지와 생략된 기본 포트를 정하고, 명시적 포트는 접속 지점만 바꾸며 스킴의 통신 방식을 바꾸지 않는다.
- 실무 연결: `https://host:8443`은 8443에서 TLS를 사용하지만 `http://host:443`은 443에 평문 HTTP를 시도하므로 TLS 서버와 통신할 수 없다.
- 핵심 교정: 처음에는 두 URL 모두 443으로 추측했으나 HTTP 기본 80과 HTTPS 기본 443을 구분했다.
- 회상 질문: 스킴과 명시적 포트가 충돌해 보이는 URL에서 실제 연결 방식과 실패 원인을 예측하라.

## Day 5 단원 3 요약

- 결과: 기준 URL과 상대 참조를 결합해 최종 절대 URL을 계산했다.
- 멘탈 모델: 상대 URL은 현재 스킴과 권한을 상속하고, 루트 상대 경로는 기존 경로를 교체하며 경로 상대 참조는 기준 경로와 병합된다.
- 실무 연결: HTML 링크, 상대 리다이렉트, 프런트엔드 API 경로는 후행 슬래시와 상위 경로 표기에 따라 예상과 다른 대상으로 해석될 수 있다.
- 핵심 교정: `cart`는 호스트가 아니라 경로 조각이며, `..` 하나는 경로 계층 한 단계만 제거한다.
- 회상 질문: 후행 슬래시가 있는 기준 URL과 없는 기준 URL에서 동일한 상대 참조의 결과를 비교하라.

## Day 5 단원 4 요약

- 결과: URL 문법과 충돌하는 공백 및 예약문자를 퍼센트 인코딩해 하나의 질의 값으로 전달했다.
- 멘탈 모델: URL 구분자는 구조를 만들고, 데이터 안에서 같은 문자가 필요하면 해당 바이트를 `%HH`로 표현해 구분자와 데이터의 경계를 보존한다.
- 실무 연결: Spring의 `@RequestParam`은 정상 인코딩된 질의 값을 디코딩해 제공하며, 문자열 연결보다 URI 빌더를 사용해 구성요소별로 인코딩해야 한다.
- 핵심 교정: 공백이 포함된 요청줄의 문법 오류는 TCP가 아니라 HTTP 파서가 검출한다.
- 회상 질문: 공백, `&`, `=`가 섞인 검색어를 단일 질의 값으로 안전하게 만들어라.

## Day 5 일일 요약

- 상태: Day 5/42 완료, 2장 진행 중
- 완료 단원:
  - 1장 통합 복습: 리소스, URI, 표현, 메시지, 연결, 중간자
  - URL의 스킴, 호스트, 포트, 경로, 질의, 프래그먼트
  - 스킴과 기본·명시적 포트
  - 절대 URL과 상대 URL 해석
  - URL 예약문자와 퍼센트 인코딩
- 확립한 내용:
  - 리소스는 URI가 식별하는 개념적 대상이며 JSON은 표현, `Content-Type`은 표현 형식 선언이다.
  - 프래그먼트는 클라이언트에서 처리되어 서버의 HTTP 요청으로 전달되지 않는다.
  - 스킴은 통신 방식을, 포트는 접속 지점을 결정하며 443이라는 번호만으로 HTTPS가 되지 않는다.
  - 상대 참조는 기준 URL과 병합되고 후행 슬래시 및 `..`가 경로 결과를 바꾼다.
  - 공백과 예약문자는 구성요소 값에서 퍼센트 인코딩하며 URL 구조 구분자는 유지한다.
- 확인된 근거:
  - 프래그먼트만 다른 URL이 서버에 같은 요청을 보냄을 설명했다.
  - `https://host:8443`과 `http://host:443`의 실제 통신 방식을 구분했다.
  - `../../cart` 상대 참조를 새 경로에 적용하고 복합 검색어를 정확히 인코딩했다.
- 보강할 내용:
  - URI, URL, URN 용어 경계
  - 프레임워크 URI 빌더의 구성요소별 인코딩과 이중 인코딩 방지
- 다음 복습:
  - 질의와 프래그먼트의 서버 전달 여부
  - 스킴과 포트의 독립된 역할
  - 후행 슬래시·`..` 상대 URL 계산
  - 예약문자가 섞인 질의 값 인코딩

## Day 6 단원 1 요약

- 결과: URL 생성 과정에서 인코딩 책임자를 하나로 정하고 이중 인코딩을 방지했다.
- 멘탈 모델: `%HH`의 `%`도 다시 인코딩 대상이므로 이미 인코딩된 값을 원본 입력용 빌더에 넣으면 `%25HH`가 되고 서버가 한 번 디코딩해도 원래 문자가 복원되지 않는다.
- 실무 연결: URI 빌더가 원본 구성요소를 받는 계약이면 `queryParam`에 원본 값을 넘기고 빌더가 한 번만 인코딩하게 한다.
- 핵심 교정: 미리 인코딩하는 것이 더 안전한 것이 아니라 빌더의 입력 계약과 인코딩 책임 경계를 지키는 것이 안전하다.
- 회상 질문: `java%2520spring`이 서버에서 한 번 디코딩된 뒤 어떤 값이 되는지 설명하라.

## Day 6 단원 2 요약

- 결과: 의미를 유지하는 범위에서 호스트, 기본 포트, 점 경로를 정규화했다.
- 멘탈 모델: URL을 문자열 전체로 임의 치환하지 않고 각 구성요소의 동등성 규칙을 적용해 표준 형태로 만든다.
- 실무 연결: 캐시 키, 요청 서명, 중복 URL 판별에서 정규화 규칙이 다르면 같은 요청을 다르게 보거나 다른 요청을 같게 보는 문제가 생길 수 있다.
- 핵심 교정: `..`는 직전 경로 조각 하나만 상쇄하며 뒤따르는 경로 조각은 유지된다.
- 회상 질문: 호스트 대문자, 기본 포트, `.`과 `..`가 섞인 URL을 단계별로 정규화하라.

## Day 6 단원 3 요약

- 결과: URI의 하위 구분인 URL과 URN을 식별 방식으로 나누고 주요 스킴의 처리 방식을 구분했다.
- 멘탈 모델: 스킴은 단순 접두사가 아니라 뒤의 식별자를 어떤 규칙과 핸들러로 처리할지 결정하며 HTTP는 가능한 처리 방식 중 하나다.
- 실무 연결: `https`는 TLS 안에서 HTTP 요청을 보내지만 `mailto`는 메일 핸들러, `file`은 파일 시스템, `data`는 식별자 안의 인라인 데이터를 사용한다.
- 핵심 교정: 프래그먼트 차이를 URN 동일성으로 표현하지 않고 전체 URL과 서버 요청 대상의 차이로 설명한다.
- 회상 질문: 새로운 스킴을 보았을 때 HTTP 요청 여부를 스킴의 처리 계약으로 판단하라.

## 2장 요약

- 개념 사슬: URI로 리소스 식별 → URL 구성요소로 접근 대상 표현 → 상대 참조를 절대 URL로 해석 → 예약문자를 퍼센트 인코딩 → 동등한 URL을 정규화 → 스킴별 처리 방식 선택.
- 핵심 불변식:
  - 스킴은 접근 방식과 기본 포트를, 호스트와 포트는 접속 지점을, 경로와 질의는 서버 요청 대상을 구성한다.
  - 프래그먼트는 클라이언트에서 처리되어 HTTP 요청으로 전달되지 않는다.
  - 상대 URL은 기준 URL 및 후행 슬래시를 바탕으로 해석되고 `..`는 직전 경로 조각 하나를 상쇄한다.
  - URL 인코딩은 구성요소 값에 한 번만 적용하며 인코딩 책임자를 하나로 정한다.
  - 정규화는 구성요소별 동등성 규칙을 적용해야 하며 임의 문자열 치환이 아니다.
- 책의 용어 구분: URI는 상위 개념, URL은 위치와 접근 방법, URN은 위치 독립적 이름으로 리소스를 식별한다.
- 현재 실무 확장: Spring URI 빌더의 입력 계약을 확인하고 원본 값과 이미 인코딩된 값을 혼용하지 않는다.
- 운영 체크:
  - 캐시 키와 요청 서명 계층이 같은 정규화 규칙을 쓰는지 확인한다.
  - 프록시와 애플리케이션의 디코딩 횟수 및 경로 정규화 차이를 경계한다.
  - URL 전체를 문자열 연결하지 않고 구성요소별 URI 빌더를 사용한다.
- 누적 회상: 복합 URL에서 정규 URL, 요청 대상, 서버 디코딩 값, 클라이언트 전용 프래그먼트를 각각 도출하라.

## Day 6 일일 요약

- 상태: Day 6/42 완료, 2장 완료
- 완료 단원:
  - 안전한 URL 생성과 이중 인코딩
  - URL 정규화
  - URI·URL·URN과 주요 스킴
  - 2장 종합 전이
- 확립한 내용:
  - URI 빌더가 인코딩을 담당하면 원본 구성요소 값을 넘겨 한 번만 인코딩한다.
  - 호스트 소문자화, 기본 포트 제거, 점 경로 제거를 구성요소별로 적용한다.
  - 스킴은 처리 방식과 핸들러를 결정하며 모든 URI가 HTTP 요청을 만들지는 않는다.
  - 전체 URL, HTTP 요청 대상, Spring의 디코딩 값은 서로 다른 관찰 단계다.
- 확인된 근거:
  - `100% spring`을 `100%25%20spring`으로 한 번만 인코딩했다.
  - 복합 경로를 `https://api.example.com/v1/orders/42`로 정규화했다.
  - 2장 종합 URL에서 정규 URL, 요청 대상, `q == "100% spring"`을 정확히 도출했다.
- 보강할 내용:
  - 프록시와 서버의 정규화·디코딩 차이가 보안에 미치는 영향
  - 사용하는 Spring URI 빌더별 인코딩 계약
- 다음 복습:
  - 이중 인코딩의 `%2520` 디코딩 결과
  - 기본 포트와 점 경로가 포함된 URL 정규화
  - URL과 URN 및 비HTTP 스킴 구분

## Day 7 단원 1 요약

- 결과: 최종 서버의 처리에 필요한 종단 간 헤더와 현재 연결에만 적용되는 hop-by-hop 헤더를 구분했다.
- 멘탈 모델: 프록시는 양쪽에 서로 독립된 연결을 가지므로 연결 제어 정보는 현재 hop에서 끝나고, 표현 협상이나 인증 정보는 담당 최종 서버까지 전달된다.
- 실무 연결: Nginx는 클라이언트의 `Connection: close`를 업스트림에 그대로 전달하지 않고 Nginx–Spring 연결의 재사용 여부를 독립적으로 관리할 수 있다.
- 핵심 교정: `keep-alive`는 단방향 개방이 아니라 요청·응답 후 양방향 연결을 다음 트랜잭션에 재사용하는 의미다.
- 회상 질문: 프록시 경로에서 `Connection`, `Accept`, `Authorization`이 어느 hop까지 전달되어야 하는지 설명하라.

## Day 7 단원 2 요약

- 결과: ETag를 이용한 조건부 GET으로 변경되지 않은 표현의 본문 재전송을 생략했다.
- 멘탈 모델: 클라이언트는 보유한 표현의 ETag를 `If-None-Match`로 보내고 서버는 현재 표현과 비교해 같으면 `304`, 다르면 `200`과 새 표현을 반환한다.
- 실무 연결: `304` 응답에 본문이 없어도 클라이언트는 기존 캐시 본문을 사용하므로 사용자에게는 정상 콘텐츠가 표시된다.
- 핵심 교정: 캐시가 있다는 이유로 서버가 같은 본문을 다시 보내는 것이 아니라 재검증 성공 시 전송을 생략해 대역폭을 절약한다.
- 회상 질문: ETag 일치와 불일치 각각에서 서버 전송 내용과 클라이언트 사용 본문을 구분하라.

## Day 7 단원 3 요약

- 결과: `If-Match` 조건부 수정을 이용해 오래된 클라이언트의 덮어쓰기를 차단했다.
- 멘탈 모델: 클라이언트가 수정 기준 ETag를 전제로 보내고 서버는 현재 ETag와 정확히 일치할 때만 변경한 뒤 새 ETag를 발급한다.
- 실무 연결: JPA `@Version`의 조건부 UPDATE와 동일하게 버전 불일치 시 수정 행 0개 또는 충돌을 감지하고 HTTP `412`로 표현할 수 있다.
- 핵심 교정: 단순히 이전 요청이 완료됐기 때문이 아니라 요청의 v7 전제와 현재 v8이 불일치하기 때문에 거부한다.
- 회상 질문: v7을 함께 읽은 두 클라이언트가 순서대로 수정할 때 ETag와 상태 코드의 변화를 설명하라.

## Day 7 단원 4 요약

- 결과: 생성과 삭제 결과를 상태 코드, 위치 헤더, 응답 표현의 유무로 명확히 전달했다.
- 멘탈 모델: 상태 코드는 처리 결과의 의미를, `Location`은 상태 코드 문맥에 따른 대상 URI를, 본문은 선택된 결과 표현을 전달한다.
- 실무 연결: 생성 API는 `201`과 새 리소스 URI를 제공하고, 삭제 API는 결과 표현이 필요하면 `200`, 필요 없으면 `204`를 사용한다.
- 핵심 교정: `201 Location`은 자동 이동 명령이 아니며 `204`는 JSON 본문과 함께 사용할 수 없다.
- 회상 질문: 생성 및 삭제 API의 상태 코드·`Location`·본문 조합이 서로 모순되지 않는지 검증하라.

## 3장 요약

- 개념 사슬: 시작줄로 동작과 결과 선언 → 헤더로 대상·협상·인증·연결·표현 메타데이터 전달 → 빈 줄로 헤더 종료 → 선택적 본문 전송 → 상태 코드와 조건부 헤더로 후속 동작 결정.
- 핵심 불변식:
  - 요청줄은 메서드·요청 대상·버전이고 상태줄은 버전·상태 코드·사유 구문이다.
  - `Host`, `Accept`, `Authorization` 같은 종단 간 정보와 `Connection` 같은 hop-by-hop 정보는 전달 범위가 다르다.
  - `304`는 서버 전송 본문이 없으며 클라이언트가 검증된 캐시 본문을 사용한다.
  - `If-Match`는 현재 ETag와 일치할 때만 변경을 허용해 lost update를 막는다.
  - 상태 코드, 헤더, 본문은 서로 모순되지 않는 하나의 응답 계약을 구성한다.
- 실무 연결:
  - Nginx는 클라이언트 연결과 Spring 업스트림 연결을 독립적으로 관리한다.
  - ETag 조건부 GET은 대역폭을 절약하고 조건부 PUT/PATCH는 JPA 낙관적 락과 유사하게 충돌을 검출한다.
  - 생성은 `201 Location`, 무본문 삭제는 `204`, 수정 전제 불일치는 `412`로 의미를 전달할 수 있다.
- 생산 점검:
  - hop-by-hop 헤더를 다음 프록시 구간에 무조건 전달하지 않는다.
  - `304`와 `204`에 애플리케이션 본문을 넣지 않는다.
  - ETag 생성 기준과 변경 시점이 실제 표현 또는 버전 정책과 일치하는지 확인한다.
- 누적 회상: 하나의 리소스에 대해 생성, 캐시 재검증, 동시 수정 충돌, 삭제까지 상태 코드·헤더·본문을 추적하라.

## Day 7 일일 요약

- 상태: Day 7/42 완료, 3장 완료
- 완료 단원:
  - 종단 간 헤더와 hop-by-hop 헤더
  - ETag와 조건부 GET 재검증
  - `If-Match`와 수정 충돌 방지
  - 생성·삭제 응답 계약
  - 3장 종합 요청·응답 추적
- 확립한 내용:
  - 프록시 구간별 연결은 각각 양방향이지만 서로 독립적으로 관리된다.
  - `Connection`은 hop-by-hop이고 `Accept` 등은 최종 서버 처리에 필요한 종단 간 정보다.
  - ETag 일치 재검증은 `304`와 캐시 본문 재사용, 불일치는 `200`과 새 표현이다.
  - `If-Match` 불일치는 `412`로 오래된 쓰기를 막는다.
  - `201 Location`은 생성 위치 식별이고 `204`에는 본문을 포함할 수 없다.
- 확인된 근거:
  - Nginx가 클라이언트의 연결 종료와 업스트림 재사용을 독립적으로 관리함을 예측했다.
  - v7·v8 충돌을 JPA 낙관적 락과 연결하고 최신 v8 재시도를 도출했다.
  - 종합 문제에서 `304`·무본문·캐시 사용과 `412`·수정 미반영을 정확히 답했다.
- 보강할 내용:
  - hop-by-hop과 독립된 연결이라는 원인·용어를 힌트 없이 설명하기
  - `401`과 `403`의 인증·권한 의미 구분
- 다음 복습:
  - 프록시 경로에서 `Connection`과 종단 간 헤더 전달 범위
  - `304`와 `412`의 ETag 조건 비교
  - `201`, `204`의 헤더·본문 계약

## Day 8 단원 1 요약

- 결과: TCP와 TLS의 역할을 분리하고 새 TCP 연결 및 HTTPS 보안 연결의 비용을 설명했다.
- 멘탈 모델: TCP는 순서·재전송을 담당하는 바이트 전달 계층이고 TLS는 그 위에서 HTTP 바이트를 암호화·검증·인증한다. `http`는 TLS만 없으며 HTTP와 TCP는 유지된다.
- 실무 연결: 지속 HTTPS 연결을 재사용하면 HTTP 트랜잭션마다 TCP 3-way handshake와 TLS 연결 수립을 반복하지 않는다.
- 핵심 교정: 연결 수립의 handshake 비용과 연결 유지의 소켓·버퍼·파일 디스크립터 비용을 구분했다.
- 회상 질문: 새 HTTPS 연결과 재사용 HTTPS 연결에서 반복되거나 생략되는 단계를 비교하라.

## Day 8 단원 2 요약

- 결과: HTTP/1.1 순차 요청, 병렬 연결, 파이프라이닝 및 HTTP/2 멀티플렉싱의 지연 특성을 비교했다.
- 멘탈 모델: 순차 방식은 앞 응답 시간이 누적되고, 파이프라이닝은 요청을 미리 보내도 응답 순서 때문에 HOL 블로킹이 남는다. HTTP/2는 스트림 ID로 프레임을 분리해 스트림 간 완료 순서를 독립적으로 허용한다.
- 실무 연결: HTTP/1.1에서는 무제한 연결 대신 제한된 수의 지속 연결을 병렬 재사용해 handshake와 소켓 비용, 순차 대기 사이를 절충한다.
- 핵심 교정: 별도 요청이라서가 아니라 별도 TCP 연결이므로 A의 지연과 독립적이며, HTTP/2는 전체 응답 순서가 아니라 각 스트림 내부 순서만 보장한다.
- 회상 질문: 느린 A가 있을 때 HTTP/1.1 순차·파이프라이닝·병렬 연결과 HTTP/2에서 B 응답 시점을 비교하라.

## Day 8 단원 3 요약

- 결과: 연결 종료 시 HTTP 응답의 완전성을 판단하고 요청 의미에 따라 안전한 재시도 여부를 결정했다.
- 멘탈 모델: TCP는 바이트와 종료를 전달하지만 HTTP만 선언된 본문 경계를 안다. 응답 유실 시 서버 처리 여부는 불확실하므로 재시도는 메서드의 멱등성과 중복 방지 계약을 기준으로 한다.
- 실무 연결: `Content-Length` 부족 또는 마지막 청크 누락은 절단으로 처리하고, 주문 생성 POST의 재시도는 최초 요청과 같은 멱등성 키로 기존 결과를 식별한다.
- 핵심 교정: 연결이 종료된 경우 다음 응답 바이트를 끌어올 수 없으며, 길이 오류로 다음 메시지를 소비하는 현상은 연결이 계속 열려 있을 때만 발생한다.
- 회상 질문: 응답 유실 후 GET과 주문 POST의 재시도 정책 및 서버 처리 불확실성을 설명하라.

## 4장 핵심 범위 중간 요약

- 개념 사슬: TCP 연결 수립 → 연결 비용과 성능 제약 → 지속 연결 재사용 → 순차·병렬·파이프라인 처리 → 연결 종료와 메시지 완전성 → 안전한 재시도.
- 핵심 불변식:
  - TCP 연결과 HTTP 트랜잭션은 1:1이 아니며 하나의 지속 연결에서 여러 트랜잭션을 처리할 수 있다.
  - 프록시 양쪽 TCP 연결은 서로 독립적이므로 한 hop의 생성·재사용·종료가 다른 hop의 동일 동작을 강제하지 않는다.
  - HTTP/1.1 파이프라이닝은 요청을 연속 전송해도 응답 순서를 유지하므로 앞 응답이 뒤 응답을 막을 수 있다.
  - TCP는 바이트와 종료를 전달하고 HTTP는 메시지 경계와 완전성을 판단한다.
  - 응답 유실 뒤 재시도는 서버 처리 여부가 불확실하므로 멱등성 및 중복 방지 계약을 따라야 한다.
- 실무 연결:
  - 제한된 수의 지속 연결을 병렬 재사용해 handshake 비용, 순차 대기, 소켓 자원 사이를 절충한다.
  - Nginx 업스트림 커넥션 풀은 클라이언트 연결과 독립적으로 Spring 연결을 재사용할 수 있다.
  - 주문 POST는 최초 시도와 같은 멱등성 키를 사용하고 서버가 원자적으로 중복 실행을 막아야 한다.
- 누적 회상: 재사용 연결에서 응답이 절단됐을 때 TCP·HTTP의 역할, 요청 처리 불확실성, 메서드별 재시도 정책을 도출하라.

## Part I 핵심 범위 중간 요약 — HTTP: 웹의 기초

- 1장: URI가 리소스를 식별하고 표현을 HTTP 트랜잭션으로 전달하며 중간자는 연결마다 서버와 클라이언트 역할을 바꾼다.
- 2장: URL의 각 구성요소는 접속 대상과 요청 대상을 만들고, 상대 참조·인코딩·정규화 규칙이 리소스 접근 결과를 결정한다.
- 3장: 시작줄·헤더·빈 줄·본문이 메시지를 구성하고 상태 코드와 조건부 헤더가 캐시·수정·생성·삭제 계약을 표현한다.
- 4장: HTTP 트랜잭션은 TCP 연결을 재사용하며 연결 방식과 종료, 메시지 경계, 멱등성이 성능과 장애 복구를 좌우한다.
- 통합 멘탈 모델: 리소스를 URL로 식별하고 HTTP 메시지로 요청·표현하며, 각 hop의 독립된 전송 연결 위에서 중간자가 이를 전달한다. 연결 장애 시 HTTP 경계와 메서드 의미를 기준으로 완료 여부와 재시도를 판단한다.

## Day 8 일일 요약

- 상태: Day 8/42 완료, 4장 핵심 범위 완료·4.2 TCP 성능 세부 미학습
- 완료 단원:
  - TCP와 TLS의 역할, 새 연결 비용, 지속 연결 재사용
  - HTTP/1.1 순차·병렬·파이프라이닝과 HTTP/2 멀티플렉싱 비교
  - 연결 종료, 본문 완전성, 안전한 재시도
  - 4장 핵심 범위 종합 전이
- 확립한 내용:
  - 새 TCP 연결은 3-way handshake가 필요하고 HTTPS는 TLS 연결 수립 비용이 더해지며 지속 연결은 이를 반복하지 않는다.
  - 제한된 수의 병렬 지속 연결은 HTTP/1.1의 순차 지연과 무제한 연결 비용을 절충한다.
  - 파이프라이닝은 요청을 미리 보내도 응답 순서 제약이 있고 HTTP/2는 스트림 ID로 스트림 간 완료 순서를 분리한다.
  - `Content-Length` 부족과 마지막 청크 누락은 HTTP가 절단으로 판단하며 close-delimited 본문은 조기 종료와 정상 종료 구분이 어렵다.
  - 비멱등 POST의 응답 유실 재시도는 같은 멱등성 키 같은 중복 방지 계약이 필요하다.
- 확인된 근거:
  - 하나의 지속 TCP 연결과 10개의 HTTP 트랜잭션을 구분했다.
  - Client-Nginx와 Nginx-Spring 연결의 재사용·종료가 독립적임을 설명했다.
  - HTTP/1.1 파이프라이닝과 HTTP/2 멀티플렉싱의 응답 순서 차이를 스트림 ID로 설명했다.
  - 종합 문제에서 잘린 POST 응답의 판단 계층, 자동 재시도 위험, 동일 멱등성 키를 정확히 도출했다.
- 보강할 내용:
  - TCP slow start, delayed ACK, Nagle 알고리즘, TIME_WAIT의 세부 성능 효과
  - HTTP/1.1의 실제 연결 풀 제한과 현대 HTTP/2·3 운영 차이
- 다음 복습:
  - 1~4장을 하나의 요청 경로와 장애 사례로 통합한다.
  - 리소스·URL·메시지·연결·중간자·재시도를 노트 없이 설명한다.

### 진도 정정 — 2026-08-25

- Day 8에는 4장의 핵심 연결 모델과 종료·재시도를 학습했지만 4.2의 slow start, delayed ACK, Nagle 알고리즘, TIME_WAIT를 학습하지 않았다.
- 따라서 Day 8의 “4장 및 Part I 완료” 표기를 “4장 핵심 범위 완료, 성능 세부 미학습”으로 정정한다.
- Day 9의 앞부분은 복습이 아니라 4.2 미학습 범위 보충이며, 보충 완료 뒤 4장·Part I 최종 통합과 누적 복습을 완료한다.

### Part I 전수 감사 — 2026-08-25

책의 1-4장 세부 목차와 학습 기록을 대조한 결과, 핵심 멘탈 모델은 학습했지만 아래 범위는 미학습 또는 언급만 하고 검증하지 않았다. 따라서 1-4장과 Part I의 최종 완료 판정을 보류한다.

- 1장 보충:
  - 하나의 웹 페이지가 여러 리소스 요청으로 구성되는 과정
  - HTTP 프로토콜 버전별 핵심 차이
  - Telnet을 이용한 실제 HTTP 요청 예시는 실습 선택 항목
- 2장 보충:
  - URL의 사용자 이름·비밀번호 구성요소
  - 경로 파라미터와 질의 문자열의 구분
  - URL 자동 확장(expandomatic URL)
  - 주요 스킴 목록은 현대 실무 관련 항목 중심으로 짧게 확인
- 3장 보충:
  - HEAD, TRACE, OPTIONS, 확장 메서드
  - 100 Continue와 1xx 처리
  - 상태 코드 계열과 주요 리다이렉션·인증 코드
  - 일반·요청·응답·엔터티 헤더 분류
  - 헤더 연속 줄과 HTTP/0.9는 역사적 맥락으로 짧게 확인
- 4장 보충:
  - TCP 연결 식별 4-tuple과 소켓 기본 모델
  - HTTP 트랜잭션 지연 요인
  - delayed ACK, slow start, Nagle/TCP_NODELAY, TIME_WAIT·포트 고갈
  - HTTP/1.0 Keep-Alive 옵션·제약, dumb proxy, Proxy-Connection 역사
  - full close·half close, FIN/RST, graceful close

완료 기준: 각 보충 범위를 학습하고 장별 전이 문제를 통과한 뒤에만 1-4장 및 Part I을 완료로 기록한다.

## Day 9 단원 1 요약

- 결과: 프록시 연결 경계, URL에서 HTTP 요청 생성, ETag 조건부 조회와 수정을 하나의 누적 회상에서 연결했다.
- 확립한 내용:
  - Spring의 `remoteAddr`는 직전 Nginx이고 XFF는 신뢰 프록시들이 관찰한 상류 주소 체인이며, `Connection`은 현재 hop에만 적용된다.
  - `https://host:8443`은 TLS를 사용하면서 8443에 연결하고 비기본 포트를 URL과 `Host`에 유지한다.
  - `If-None-Match` 일치는 `304`, 불일치는 `200`과 새 표현이며, `If-Match` 불일치는 `412`로 수정을 거부한다.
- 확인된 근거: ETag v7 캐시와 현재 v8 사례에서 GET은 `200`·v8 본문, PUT은 `412`·수정 거부를 정확히 도출했다.
- 보강점: 명시적 포트와 요청 메시지 토큰을 처음부터 정확히 쓰고, 조건부 헤더 이름을 상태 코드보다 먼저 확인한다.
- 회상 질문: 비기본 HTTPS 포트의 URL·요청줄·Host와 ETag 조회·수정 조건을 한 사례에서 도출하라.

## Day 9 단원 2 요약

- 결과: HTTP 트랜잭션 완료, 서버 애플리케이션 처리, 응답 전달, 클라이언트 결과 확정을 서로 다른 판단으로 분리했다.
- 확립한 내용:
  - 완전한 `404`는 애플리케이션 결과가 Not Found여도 HTTP 트랜잭션은 완료다.
  - 선언된 길이보다 짧은 응답은 HTTP가 절단으로 판단하지만 서버 로직의 성공·실패까지 단정하지 않는다.
  - 연결이 종료되면 부족한 바이트를 더 받을 수 없고, 연결이 유지되면 파서가 다음 응답 앞부분을 이전 본문으로 소비해 메시지 경계를 오염시킬 수 있다.
  - 표준 GET은 재시도 가능하고, 주문 POST는 동일 멱등성 키와 서버의 원자적 중복 방지가 필요하다.
- 확인된 근거: 서버 처리 성공과 완전한 표현 수신을 분리하고 잘못된 Content-Length가 다음 응답의 시작줄을 훼손하는 과정을 설명했다.
- 보강점: 서버 처리 결과와 클라이언트 관찰 결과를 ‘성공/실패’ 한 단어로 합치지 않고 계층별로 표현한다.
- 회상 질문: 완전한 오류 응답, 절단 GET, 응답 유실 POST의 완료·결과 확정·재시도를 비교하라.

## Day 9 보충 단원 — 1장 누락 범위

- 결과: 사용자 관점의 웹페이지, HTTP 리소스·트랜잭션, TCP 연결 수를 분리하고 버전별 기본 연결 정책을 적용했다.
- 확립한 내용:
  - HTML이 CSS·JavaScript·이미지를 참조하면 브라우저는 각 리소스에 별도 HTTP 요청을 수행한다.
  - HTTP/1.0은 기본 종료, HTTP/1.1은 기본 지속 연결이며 연결 정책은 hop마다 독립적이다.
  - HTTP/1.0+는 Keep-Alive 같은 비공식 확장을 포함한 관용적 표현이다.
  - Telnet은 TCP 위의 HTTP 텍스트를 직접 관찰하기 위한 도구다.
- 확인된 근거: 5개 리소스에서 HTTP/1.0 기본은 최소 연결 5개, HTTP/1.1 지속 연결은 연결 1개·트랜잭션 5개임을 도출했다.
- 회상 질문: 페이지·리소스·트랜잭션·연결의 수와 HTTP/1.0·1.1 기본 연결 정책을 비교하라.

## Day 9 보충 단원 — 2장 누락 범위

- 결과: URL 권한부의 사용자 정보, 세미콜론 경로 파라미터, 클라이언트 URL 자동 확장, 비HTTP 스킴을 구분했다.
- 확립한 내용:
  - userinfo는 URL에 자격 증명을 노출하므로 현대 서비스에서 사용하지 않는다.
  - `/books;lang=ko/42;edition=2?q=spring#review`에서 경로 조각별 파라미터·질의·프래그먼트는 서로 다른 문법 경계를 가진다.
  - 브라우저가 축약 URL을 확장한 뒤 일반적인 원본 서버 요청줄에는 경로·질의만, `Host`에는 호스트와 비기본 포트가 들어간다.
  - 스킴이 다르면 HTTP가 아닌 별도 프로토콜이나 로컬 핸들러가 실행될 수 있다.
- 확인된 근거: 복합 URL에서 userinfo, TLS·8443, 순수 경로, `lang=ko`, 요청 대상, 프래그먼트를 도출하고 비기본 포트를 `Host`에 포함하도록 교정했다.
- 회상 질문: userinfo·세미콜론 파라미터·질의·프래그먼트·비기본 포트가 섞인 URL을 HTTP 요청으로 변환하라.

## Day 9 보충 단원 — 3장 메서드·상태 코드·헤더 분류

- 결과: 본문 없는 메타데이터 조회, 서버 기능 확인, 요청 경로 진단, 대용량 본문 전송 협상과 주요 상태 코드·헤더 분류를 구분했다.
- 확립한 내용:
  - `HEAD`는 GET과 같은 표현 메타데이터를 본문 없이 확인하고, `OPTIONS`는 지원 기능을, `TRACE`는 최종 서버가 받은 HTTP 요청 메시지를 반사한다.
  - `Via`는 통과한 HTTP 중간자와 수신 프로토콜 버전의 체인이며 XFF 주소 체인 및 소켓의 `remoteAddr`와 다르다.
  - `100 Continue`는 본문 전송을 계속하라는 임시 응답이고 최종 성공·실패는 뒤따르는 non-1xx 상태가 결정한다.
  - 비동기 작업이 접수만 됐으면 `202`, 범위 응답은 `206`, 메서드와 본문을 보존하는 임시 이동은 `307`을 사용한다.
  - 일반·요청·응답·엔터티 헤더는 역할 분류이고 end-to-end·hop-by-hop은 전달 범위 분류이므로 서로 다른 축이다.
  - `Content-Type`은 요청과 응답 모두에서 본문의 형식을 나타내는 엔터티 헤더이며 일반적으로 최종 수신자까지 의미가 유지되는 end-to-end 헤더다.
- 핵심 교정: end-to-end는 바이트 단위 원본 불변이 아니라 최종 수신자까지 의미가 전달되는 범위이며, 중간자가 표현을 변환하면 관련 헤더도 일관되게 변경할 수 있다.
- 확인된 근거: HEAD·OPTIONS·TRACE, 100과 415, 201과 202, Range와 206, 302·303·307, 401·403·405·415·504를 사례별로 정확히 구분했다.
- 3장 보충 완료: 헤더 연속 줄과 HTTP/0.9의 역사적 맥락까지 확인했다.
- 회상 질문: 대용량 JSON 업로드가 프록시를 통과할 때 `Expect`, `Content-Type`, `Connection`, 최종 상태 코드의 역할과 전달 범위를 구분하라.

## Day 9 보충 단원 — 3장 역사적 문법

- 결과: 과거의 헤더 연속 줄과 HTTP/0.9 메시지를 현대 HTTP/1.x 문법과 구분했다.
- 멘탈 모델: 과거 HTTP/1.x의 공백·탭 시작 줄은 이전 헤더 값의 연속으로 합쳐졌고, HTTP/0.9 응답은 상태줄과 헤더 없이 본문만 전송했다.
- 핵심 교정: 헤더 연속 줄은 현대 메시지 생성에 사용하지 않는 폐기된 문법이며, HTTP/0.9는 현재 애플리케이션 프로토콜 설계 기준이 아니라 역사적 출발점이다.
- 실무 연결: 중간자마다 줄바꿈을 다르게 해석하면 요청 스머글링 같은 파싱 불일치 위험이 생기므로 현대 서버·프록시는 모호한 메시지를 거부하거나 정규화해야 한다.
- 확인된 근거: `X-Role: user` 다음의 공백 시작 `admin`을 `user admin`으로 결합하고, 상태줄과 `Content-Type`이 있는 응답을 HTTP/1.x로 판별했다.
- 회상 질문: 상태줄·헤더가 없는 본문 전용 응답과 공백으로 시작하는 헤더 줄을 현대 프록시가 그대로 허용하면 어떤 모호성이 생기는지 설명하라.

## Day 9 보충 단원 — TCP 4-tuple과 소켓

- 결과: 하나의 서버 포트가 여러 TCP 연결을 수용하고 운영체제가 연결별 소켓을 구분하는 기준을 설명했다.
- 멘탈 모델: 리스닝 소켓은 특정 로컬 주소·포트에서 연결을 받고, 수립된 각 TCP 연결은 출발지 IP·출발지 포트·목적지 IP·목적지 포트의 4-tuple로 식별되어 별도의 연결 소켓과 FD로 관리된다.
- 핵심 교정: TCP 연결은 HTTP 헤더가 아니라 HTTP 메시지를 받기 전부터 4-tuple로 구분된다. 동시 연결 한계도 FD 하나만이 아니라 메모리·소켓 버퍼·커널 설정 등 여러 자원의 영향을 받는다.
- 실무 연결: NAT 뒤의 여러 클라이언트가 같은 공인 IP를 공유해도 NAT가 변환한 출발지 포트가 다르면 서버 관점의 4-tuple이 달라 별도 연결이 된다.
- 확인된 근거: 동일한 공인 IP와 목적지 443을 사용하는 두 연결을 서로 다른 출발지 포트로 구분했다.
- 회상 질문: 같은 서버 443 포트에 접속한 여러 클라이언트를 HTTP 헤더 없이 운영체제가 어떻게 구분하는지 설명하라.

## Day 9 보충 단원 — 트랜잭션 지연과 Delayed ACK

- 결과: HTTP 애플리케이션 처리 지연과 TCP ACK 지연을 계층별로 분리했다.
- 멘탈 모델: TCP ACK는 상대 TCP가 특정 순서 번호까지의 바이트를 수신했다는 누적 확인이며, Spring의 요청 처리 완료를 의미하지 않는다. Delayed ACK는 ACK 전용 패킷을 줄이기 위해 잠시 기다렸다가 반대 방향 데이터와 함께 보내려는 전송 계층 최적화다.
- 핵심 교정: ACK는 JSON 유효성이나 비즈니스 처리 성공을 확인하지 않으며, 서버 커널의 수신 확인과 애플리케이션 응답 생성은 독립된 타임라인이다.
- 실무 연결: Spring의 DB 조회가 10초 걸리더라도 서버 커널은 요청 바이트에 대한 ACK를 먼저 보낼 수 있다. 작은 쓰기에서 송신자도 ACK를 기다리고 수신자도 ACK를 지연하면 애플리케이션 로직과 무관한 짧은 정체가 생길 수 있다.
- 확인된 근거: ACK 완료를 TCP 바이트 수신 확인으로 좁히고 HTTP 요청 처리는 아직 완료되지 않았다고 구분했다.
- 회상 질문: 서버 커널이 요청 ACK를 보낸 뒤 Spring이 10초 동안 처리 중인 상황에서 TCP와 HTTP 각각 무엇이 완료됐는지 설명하라.

## Day 9 보충 단원 — Slow Start와 Nagle/TCP_NODELAY

- 결과: 새 TCP 연결의 전송량 증가 과정과 작은 쓰기의 지연·효율 절충을 설명했다.
- 멘탈 모델: Slow Start는 새 연결이 작은 혼잡 윈도우에서 시작해 ACK 피드백을 받으며 현재 네트워크 경로가 감당할 수 있다고 추정하는 미확인 데이터 양을 늘리는 과정이다. Nagle은 미확인 데이터가 있을 때 작은 쓰기를 잠시 모아 작은 세그먼트 남발을 줄이고, `TCP_NODELAY`는 이를 비활성화해 작은 데이터를 가능한 한 즉시 보낸다.
- 핵심 교정: 워밍된 연결이 파악한 것은 서버 처리 한계가 아니라 네트워크 경로의 전송 여력이며, `TCP_NODELAY`는 RTT 자체를 늘리는 것이 아니라 세그먼트 수와 TCP/IP 헤더·패킷 처리 오버헤드를 늘릴 수 있다.
- 실무 연결: 실시간 게임·채팅처럼 작은 메시지의 지연이 중요한 경우 `TCP_NODELAY`를 선택할 수 있고, 대용량 스트리밍처럼 처리량 효율이 중요한 경우에는 작은 쓰기를 애플리케이션에서 합치는 전략도 고려한다.
- 확인된 근거: 새 연결보다 ACK 피드백으로 혼잡 윈도우가 확장된 지속 연결이 초반 전송에 유리함을 설명하고, 실시간 게임에서 `TCP_NODELAY`의 지연 이점과 패킷·프로토콜 오버헤드 비용을 함께 도출했다.
- 회상 질문: 새 TCP 연결에서 작은 게임 메시지를 연속 전송할 때 Slow Start와 Nagle이 각각 어떤 지연을 만들 수 있으며 `TCP_NODELAY`는 무엇을 바꾸는가?

## Day 9 보충 단원 — TIME_WAIT와 출발지 포트 고갈

- 결과: TIME_WAIT의 안전 목적과 짧은 연결 반복 시 임시 출발지 포트가 고갈되는 위치를 각 TCP hop별로 설명했다.
- 멘탈 모델: 연결을 먼저 종료한 측은 지연된 이전 세그먼트가 같은 4-tuple의 새 연결에 섞이는 것을 막고 마지막 ACK 재전송 가능성을 남기기 위해 일정 시간 연결 상태를 보존한다. 같은 목적지 IP·포트로 새 연결을 반복하면 연결을 여는 측의 임시 출발지 포트가 계속 바뀐다.
- 핵심 교정: 서버의 목적지 포트는 연결마다 소비되는 자원이 아니라 여러 연결이 공유하는 리스닝 포트다. 포트 고갈은 인터넷 전체 클라이언트의 포트를 합친 문제가 아니라 특정 출발지 IP에 연결이 집중되는 프록시·NAT·클라이언트의 임시 포트 공간 문제다.
- 실무 연결: Client-L7 LB, L7 LB-Nginx, Nginx-Spring은 독립된 TCP 연결이며 각 hop에서 다음 연결을 여는 프록시가 출발지가 된다. 연결 풀과 지속 연결 재사용은 handshake뿐 아니라 TIME_WAIT 및 임시 포트 압력도 줄인다.
- 확인된 근거: `Nginx:53000 → Spring:8080`에서 Nginx에 TIME_WAIT가 쌓이면 재사용이 제한되는 쪽은 Nginx의 `53000`임을 4-tuple로 설명했다.
- 회상 질문: 하나의 L7 LB 출발지 IP가 같은 Nginx 주소로 짧은 연결을 반복할 때 어느 포트 공간이 소모되며 목적지 리스닝 포트는 왜 고갈되지 않는가?

## Day 9 보충 단원 — HTTP/1.0 Keep-Alive와 프록시

- 결과: HTTP/1.0 지속 연결 확장의 동작과 dumb proxy가 hop-by-hop 연결 정책을 잘못 전달할 때 발생하는 대기 문제를 설명했다.
- 멘탈 모델: `Connection`은 현재 TCP hop에서만 의미가 있으므로 프록시가 소비해야 한다. Client-Proxy와 Proxy-Origin은 독립된 연결이며 한쪽을 유지하더라도 다른 쪽은 종료할 수 있다.
- 핵심 교정: dumb proxy의 상호 대기는 요청 크기나 요청 제한 오류가 아니라 timeout을 만들며, 프록시는 반드시 `Connection: close`로 바꾸는 것이 아니라 해당 헤더를 다음 hop에 그대로 전달하지 않고 각 연결 정책을 따로 결정해야 한다.
- 역사적 맥락: `Proxy-Connection`은 이를 아는 프록시가 Client-Proxy 정책으로 소비하고 Origin은 모르는 헤더로 무시하게 만든 비표준 우회책이다. 여러 프록시 중 하나가 이를 `Connection`으로 바꾸고 다음 dumb proxy가 전달하면 같은 문제가 재발할 수 있다.
- 확인된 근거: Client-Proxy는 Keep-Alive로 유지하면서 Proxy-Origin은 응답 후 종료할 수 있으며 그 이유가 hop별 독립 연결 정책임을 설명했다.
- 회상 질문: 다중 프록시 경로에서 `Connection: Keep-Alive`를 그대로 전달하면 왜 양쪽의 메시지 종료 판단이 충돌할 수 있는가?

## Day 9 보충 단원 — 연결 종료와 graceful close

- 결과: TCP 양방향 스트림의 half close와 full close를 구분하고 FIN·RST가 데이터 완전성에 미치는 차이를 설명했다.
- 멘탈 모델: TCP 연결은 서로 독립적인 두 방향의 바이트 스트림이다. 한쪽이 출력 방향에 FIN을 보내도 반대 방향 데이터는 계속 받을 수 있으며, 양쪽이 각자 전송을 마쳐야 정상적인 전체 종료가 완성된다.
- 핵심 교정: `close()`가 언제나 RST를 만드는 것은 아니다. 아직 읽지 않은 수신 데이터가 남은 채 연결을 강제로 폐기하면 운영체제 설정과 상황에 따라 RST가 발생해 상대가 정상 전달 완료를 확신하지 못할 수 있다.
- 실무 연결: graceful close는 자신의 전송을 마치고 출력 방향을 종료한 뒤 상대의 남은 데이터를 FIN까지 읽되, 상대가 영원히 닫지 않는 상황을 막기 위해 timeout을 둔다.
- 확인된 근거: Nginx가 Spring 방향에 FIN을 보낸 뒤에도 Spring 응답을 계속 읽는 상태를 half close로 판별하고, 남은 수신 데이터를 안전하게 처리하려고 전체 소켓을 즉시 닫지 않는다고 설명했다.
- 회상 질문: 응답을 읽지 않은 즉시 종료와 출력 FIN 후 상대 FIN까지 읽는 종료에서 상대가 관찰하는 결과와 데이터 손실 가능성을 비교하라.

## Part I 1-4장 최종 요약 — HTTP: 웹의 기초

- 개념 사슬: 리소스와 표현 → URL 식별·해석 → HTTP 메시지와 메서드·상태·헤더 계약 → TCP 연결의 수립·재사용·성능·종료.
- 핵심 불변식:
  - URL의 프래그먼트는 서버에 전달되지 않고 비기본 포트는 연결 대상과 `Host`에 유지된다.
  - 요청 경로의 각 프록시는 독립된 TCP hop을 만들며 `remoteAddr`는 직전 상대, XFF·`Forwarded`는 신뢰 프록시가 기록한 상류 정보다.
  - 역할별 헤더 분류와 end-to-end·hop-by-hop 전달 범위는 서로 다른 축이다.
  - 조건부 요청은 현재 validator와 조건값을 비교해 `304`, `200`, `412`를 결정한다.
  - TCP는 바이트 전달과 방향별 종료를 담당하고 HTTP는 메시지 경계·완전성·메서드 의미를 판단한다.
  - 지속 연결은 handshake, slow start, TIME_WAIT와 임시 포트 압력을 줄이지만 연결 자원을 점유한다.
- 역사·현재 구분: HTTP/0.9, 헤더 연속 줄, HTTP/1.0 Keep-Alive와 `Proxy-Connection`은 역사적 문맥이며, 현대 중간자는 모호한 문법을 거부하고 hop별 연결 정책을 명확히 처리해야 한다.
- 실무 점검:
  - CloudFront·ALB·Nginx·Spring 각 hop의 연결 풀, timeout, 전달 헤더와 신뢰 경계를 따로 확인한다.
  - 잘린 응답은 Content-Length·청크 경계로 판별하고 재시도는 멱등성과 중복 방지 계약을 따른다.
  - 다량의 `TIME_WAIT`는 짧은 연결 반복, `CLOSE_WAIT`는 상대 FIN 이후 로컬 종료 누락 가능성을 우선 조사한다.
  - 작은 실시간 메시지는 `TCP_NODELAY`의 지연 이점과 패킷 오버헤드를 함께 평가한다.
- 누적 회상: 비기본 HTTPS URL 요청이 여러 프록시를 거쳐 조건부 응답을 받고 절단되는 상황에서 URL·헤더·연결·완전성·재시도 판단을 계층별로 설명하라.

## Day 9 일일 요약

- 상태: Day 9/42 완료, Part I 1-4장 전수 감사와 누락 보충 및 최종 통합 완료.
- 완료 범위:
  - 1장 복합 웹페이지·HTTP 버전·Telnet
  - 2장 userinfo·경로 파라미터·URL 확장·스킴
  - 3장 메서드·1xx·주요 상태·헤더 분류·역사적 문법
  - 4장 4-tuple·Delayed ACK·Slow Start·Nagle·TIME_WAIT·HTTP/1.0 Keep-Alive·연결 종료
  - Part I 최종 전이
- 확인된 근거:
  - URL에서 요청줄·Host와 프래그먼트 전달 경계를 정확히 도출했다.
  - 직전 TCP 상대와 전달 IP 헤더를 구분하고 hop별 연결 정책을 설명했다.
  - ETag 불일치를 교정해 `If-None-Match`의 `200`·`304` 조건을 다시 설명했다.
  - Content-Length보다 짧은 응답을 절단으로 판별하고 Nginx의 TIME_WAIT·임시 출발지 포트 압력을 연결했다.
  - FIN half close, RST 위험과 CLOSE_WAIT 누수의 차이를 실무 Feign 상황까지 확장했다.
- 보강할 내용: validator 값을 먼저 비교한 뒤 상태 코드를 결정하는 습관을 유지한다.
- 다음 복습: 2026-08-31에 Part I 통합 경로를 노트 없이 재구성한다.
- 다음 학습: Day 10/42, Part II 5장 웹 서버 아키텍처.

## Day 10 단원 1 요약 — 웹 서버 요청 처리 경계

- 결과: 원시 TCP 바이트가 Spring Controller 실행과 HTTP 응답 전송으로 이어지는 계층별 책임을 설명했다.
- 멘탈 모델: 커널은 TCP 연결·바이트를 관리하고 Tomcat은 연결 수락·HTTP 파싱·Servlet 요청/응답 생성과 Servlet 호출을 담당한다. Spring MVC는 DispatcherServlet 흐름에서 라우팅·컨트롤러 실행·표현 변환을 수행하며 Tomcat이 최종 HTTP 바이트를 소켓으로 전송한다.
- 핵심 교정: Tomcat의 리스닝 포트는 애플리케이션 시작 시 열리며 요청마다 새로 열리지 않는다. DispatcherServlet은 Spring이 임의 실행하는 것이 아니라 Tomcat이 등록된 Servlet로 호출한다.
- 실무 연결: Controller 반환 객체는 Spring의 `HttpMessageConverter`가 JSON으로 변환하고, 상태줄·헤더·본문의 실제 네트워크 전송은 Servlet 컨테이너와 커널 경계를 거친다.
- 확인된 근거: `OrderResponse`의 JSON 변환은 Spring, HTTP 응답 바이트 전송은 Tomcat으로 구분했다.
- 회상 질문: 요청 바이트 수신부터 Controller 반환 객체의 JSON 응답 전송까지 커널·Tomcat·Spring의 책임을 순서대로 설명하라.

## Day 10 단원 2 요약 — 동시 연결 처리와 I/O 모델

- 결과: 웹 서버의 단일 실행 흐름, 요청별 실행 흐름, 다중화 I/O와 워커 풀 구조의 병목 위치를 설명했다.
- 멘탈 모델: 다중화 I/O는 소수의 Poller가 많은 소켓의 준비 상태를 감시하게 하지만, 준비된 요청의 비즈니스 처리는 워커에서 실행된다. 동기 JDBC 대기는 해당 실행 스레드를 계속 점유하므로 네트워크 NIO만으로 애플리케이션 블로킹이 사라지지 않는다.
- 핵심 교정: Spring MVC의 워커 풀 고갈과 Netty 이벤트 루프 블로킹은 다른 실패 형태다. WebFlux에서 JDBC를 이벤트 루프에 직접 실행하면 소수 이벤트 루프가 막혀 더 넓은 연결에 영향을 줄 수 있다.
- 실무 연결: JDBC를 `boundedElastic`에 격리하면 이벤트 루프는 보호되지만 처리량은 제한된 작업 스레드와 DB 커넥션 풀에 의존한다. R2DBC는 DB 응답 대기 중 요청 전용 스레드를 점유하지 않아 end-to-end 논블로킹 흐름을 구성할 수 있다.
- 확인된 근거: `WebFlux+JDBC+boundedElastic`은 블로킹 작업을 별도 풀로 옮긴 것이고, `WebFlux+R2DBC`는 DB 대기 동안 스레드를 붙잡지 않는다고 구분했다.
- 회상 질문: Tomcat NIO, Spring MVC 워커, Netty 이벤트 루프, boundedElastic, R2DBC에서 DB 10초 지연이 각각 어느 자원을 점유하는지 비교하라.

## Day 10 단원 3 요약 — 리소스 매핑과 가상 호스팅

- 결과: 같은 IP의 여러 사이트를 Host로 구분하고 요청 경로를 정적 파일 또는 동적 핸들러에 안전하게 매핑했다.
- 멘탈 모델: 서버는 먼저 `Host`로 가상 호스트·애플리케이션을 선택한 뒤 요청 경로를 해당 사이트의 document root 또는 애플리케이션 라우팅 규칙에 연결한다. document root는 외부에 공개하도록 허용한 서버 파일 트리의 최상위 경계다.
- 핵심 교정: 경로의 `../` 문자열만 먼저 검사하면 인코딩으로 우회될 수 있다. 정해진 규칙으로 디코딩하고 점 경로를 정규화한 최종 경로가 document root 내부인지 같은 해석 결과로 검증해야 한다.
- 실무 연결: 정적 `/css/app.css`는 파일로 매핑될 수 있고 동적 `/orders/42`는 Tomcat의 HTTP 파싱과 DispatcherServlet 호출 후 HandlerMapping을 통해 Controller에 매핑된다.
- 확인된 근거: `/srv/web/public/assets/../../config.yml`을 `/srv/web/config.yml`로 정규화해 document root 밖이므로 거부하고, Host 선택 뒤 Spring 라우팅 순서를 설명했다.
- 회상 질문: 같은 IP의 두 도메인에서 Host·요청 경로·document root·HandlerMapping이 리소스를 선택하는 순서를 설명하라.

## Day 10 단원 4 요약 — MIME 타입·리다이렉션·접근 로그

- 결과: 서버가 표현 메타데이터와 리다이렉션 응답을 구성하고 프록시 경계에서 신뢰할 수 있는 접근 로그를 만드는 방법을 설명했다.
- 멘탈 모델: `Content-Type`은 클라이언트가 본문 표현을 해석할 때 사용하는 계약이며 실제 바이트와 일치해야 한다. 리다이렉션은 `Location`으로 새 URI를 알려 클라이언트가 두 번째 요청을 보내게 한다. 접근 로그는 요청·응답 결과뿐 아니라 어느 프록시가 전달 주소를 만들었는지의 신뢰 경계를 포함해야 한다.
- 핵심 교정: 본문 모양이 JSON이어도 `text/plain`이면 Feign의 JSON Decoder 선택을 보장할 수 없다. XFF·`Forwarded`는 문자열 자체가 믿을 만한 것이 아니라 외부 입력을 제거·덮어쓴 신뢰 프록시가 만든 범위만 신뢰할 수 있다.
- 실무 연결: `/docs`에서 `/docs/`로의 리다이렉션은 상대 URL 기준을 바로잡고, ALB·Nginx·Spring 경로에서는 `remoteAddr`와 전달 주소, 요청 ID, 상태, 바이트, 처리 시간, upstream 시간을 함께 기록해 장애를 추적한다.
- 확인된 근거: 신뢰하는 ALB가 위조 XFF를 제거하고 관찰 주소 `1.1.1.1`로 덮어쓴 경우 그 값을 신뢰하며, 근거는 헤더 문자열이 아니라 trusted proxy 설정이라고 설명했다.
- 회상 질문: 잘못된 Content-Type, `/docs` 리다이렉션, 위조 XFF가 한 요청 경로에 있을 때 표현·위치·클라이언트 주소의 신뢰 기준을 각각 설명하라.

## 5장 최종 요약 — 웹 서버

- 개념 사슬: TCP 연결 수락 → HTTP 요청 파싱 → 가상 호스트와 리소스 매핑 → 요청 처리 → 표현 메타데이터와 응답 생성 → 전송 → 트랜잭션 로깅.
- 핵심 경계:
  - 커널은 TCP 연결과 바이트를 관리하고 Tomcat은 HTTP 파싱·Servlet 요청/응답·Servlet 호출을 담당한다.
  - Spring MVC는 DispatcherServlet 흐름에서 HandlerMapping·HandlerAdapter·Controller·HttpMessageConverter를 조정한다.
  - Host는 가상 호스트·애플리케이션을 선택하고 요청 경로는 그 내부의 정적 파일 또는 동적 핸들러를 선택한다.
  - document root는 공개 가능한 파일 트리의 경계이며 최종 정규 경로가 그 내부인지 검증해야 한다.
  - Content-Type은 표현 해석 계약이고 Location은 클라이언트가 새 URI로 후속 요청하도록 안내한다.
- 동시성·성능:
  - 다중화 I/O는 소수 Poller로 많은 소켓을 감시하지만 동기 비즈니스 작업의 워커 점유를 제거하지 않는다.
  - Tomcat NIO에서도 JDBC가 10초 대기하면 MVC 워커가 점유된다.
  - WebFlux에서 JDBC를 이벤트 루프에 직접 실행하면 이벤트 루프가 막히며, boundedElastic은 블로킹을 별도 제한 풀로 옮길 뿐이다.
  - R2DBC는 DB 응답 대기 중 요청 전용 스레드를 점유하지 않는 논블로킹 흐름을 지원한다.
- 보안·운영:
  - 디코딩·정규화와 다른 기준으로 경로를 검사하면 path traversal 우회가 발생할 수 있다.
  - XFF·Forwarded는 헤더 문자열 자체가 아니라 외부 값을 정리한 trusted proxy의 경계 안에서만 신뢰한다.
  - 접근 로그에는 요청 ID, 메서드, URI, 상태, 바이트, 처리 시간, upstream 시간과 신뢰 가능한 클라이언트 주소가 필요하다.
- 누적 회상: 하나의 요청이 ALB·Nginx·Tomcat·Spring MVC·JDBC를 거쳐 응답될 때 연결, 파싱, 라우팅, 실행, 표현, 로깅의 책임과 병목을 순서대로 설명하라.

## Day 10 일일 요약

- 상태: Day 10/42 완료, Part II 5장 웹 서버 완료.
- 완료 단원:
  - 웹 서버 요청 처리 경계
  - 동시 연결 처리와 I/O 모델
  - 리소스 매핑·document root·가상 호스팅
  - MIME 타입·리다이렉션·접근 로그
  - 5장 최종 통합 전이
- 확인된 근거:
  - Tomcat의 HTTP 파싱·Servlet 호출과 Spring MVC의 Controller 선택·JSON 변환을 구분했다.
  - 500개 요청과 200개 워커 사례에서 동기 JDBC의 큐 대기·timeout을 예측했다.
  - 인코딩 경로를 정규화해 document root 외부 접근을 거부했다.
  - Host와 경로를 결합하지 않고 서로 다른 선택 축으로 설명했다.
  - ALB가 덮어쓴 1.1.1.1을 신뢰하고 공격자가 넣은 127.0.0.1을 거부했다.
- 보강할 내용: 127.0.0.1 같은 주소의 의미와 전달 헤더의 진위를 분리해 판단한다.
- 다음 복습: 2026-09-06에 5장 요청 처리 파이프라인과 블로킹 병목을 노트 없이 재구성한다.
- 다음 학습: Day 11/42, 6장 프록시.

## 다음 학습

- Day 10/42 및 5장 완료
- Day 11/42 시작
- Part II 6장: 프록시의 역할, 배치, 트래픽 처리와 메시지 변경
- 시작 회상: Tomcat NIO 서버에서 동기 JDBC가 느릴 때 네트워크 Poller와 요청 Worker의 상태를 구분하기
